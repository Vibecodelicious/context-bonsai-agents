# Story: Forward-port cycle — Kilo Context Bonsai onto upstream `v7.4.1` (Stage 6 pressure test)

Routine forward-port cycle per `docs/agent-specs/forward-port-spec.md` (Part 1 core, Part 2 shape binding — git-fork, §4.7 Kilo slot table). Generated by the owner tier on 2026-07-06 from the spec plus the §4.7 slots. This is the **first routine cycle for the Kilo harness** and the Stage 6 acceptance-gate plan (`derivation-pipeline-spec.md` §9): the §4.7 binding is proven by the routine path consuming it. All relative paths are from the parent repo root (`context-bonsai-agents/`); worktree/side-repo commands state their working directory.

## Goal

Replay the Kilo Context Bonsai port — the contiguous 3-commit `[CB-kilo.1]` suffix tipped at `SOURCE_HEAD_SHA=feb40128460987971f9b98dc34fff515226ff83c` on `feat/context-bonsai-port` — onto the frozen upstream release tag `v7.4.1` in an isolated worktree, and prove the §4.7 git-fork machinery (deterministic inventory, classification, cherry-pick replay, canonical validation set) runs clean against it. This is an `uncommitted-pressure-test` cycle against a **real pending release**: `v7.4.1` is the detector's live pending-target signal for Kilo (`ported=7.2.20 upstream=7.4.1`, 2026-07-06), but per the Stage 6 acceptance gate the cycle stops short of landing anything. Nothing is pushed and no durable ref (branch/tag) is created; the worktree replay commits are scratch verified by working-tree/provenance commands (§1.11).

## Non-Goal

- No landing of the forward-port: no durable branch or tag, no §2.9 publish ladder (no `bonsai/v1-on-kilo-7.4.1` tag, no pin advance, no push, no `main` fast-forward). The real v7.4.1 forward-port cycle runs later under §1.20 cadence (kilo next eligible 2026-07-13) and will regenerate from the then-current source head.
- No live e2e: per §1.15's rehearsal scope and the pressure-test purpose, the E2E gate rows are **bound but not run** (marked `not-run-in-pressure-test`). Live e2e — including the open E2E-04 gauge scenario the §4.7 slot table requires of the first cycle that reaches the live gate — is out of scope here.
- No replay/rewrite of the source branch in place; all replay happens in the scratch worktree.
- No side-repo (`kilo_context_bonsai/`) code changes: no replay-set row binds any.
- This plan is generated only. Generation does not create the worktree, build, or commit anything; the executor performs those on execution.

## Execution Outcome Statement

After execution, the scratch worktree's `HEAD` is based on `UPSTREAM_HEAD_SHA` (the `v7.4.1` commit) and contains exactly the 3 replayed commits from the approved replay set in source topological order — verified by cherry-pick provenance (`-x` trailers) and working-tree/diff commands, never by source-SHA equality. No durable branch or tag is pushed; the worktree and its sibling symlink shim are removed at seal (§1.19). The pressure test passes when the §1.15 generation validation loop (including the target-resolution rehearsal) returns zero blocking findings and the post-replay canonical validation set is green in rehearsal.

## Frozen Inputs (§1.1)

- `SOURCE_REF`: `refs/heads/feat/context-bonsai-port`
- `SOURCE_HEAD_SHA`: `feb40128460987971f9b98dc34fff515226ff83c`
- `UPSTREAM_REF`: `refs/tags/v7.4.1` (fetched explicitly 2026-07-06 — the upstream remote's refspec is heads-only, §4.7)
- `UPSTREAM_HEAD_SHA`: `4ed43ab7c5309761276a0513fff99019df1d0570` (`git rev-parse v7.4.1`)
- `BASE_SHA`: `415dc311fd16bd619bb8bff9161274ff18582b8f` — **computed, not asserted** (§2.1): `git merge-base v7.4.1 feat/context-bonsai-port` at generation; `415dc311fd` is an ancestor of `v7.4.1` (`merge-base --is-ancestor` confirmed), so the inventory range contains no upstream commits.
- `TARGET_WORKTREE`: `/home/basil/projects/context-bonsai-agents/kilo/.agent_tmp/rebase-on-v7.4.1`
  - **Sibling symlink shim** (§4.7 Naming): before any post-replay validation in the worktree, create `kilo/.agent_tmp/kilo_context_bonsai` → `../../kilo_context_bonsai`. The replayed config's plugin path (`../../kilo_context_bonsai/src/plugin.ts`, relative to `<worktree>/.opencode/`) and the seam test both resolve two levels above the config file, which from the worktree lands in `.agent_tmp/`. Step-scoped scratch, removed with the worktree per §1.19. Clean-upstream baseline rows need no shim.
- `TARGET_BRANCH`: `replay/context-bonsai-on-kilo-7.4.1` — scratch worktree branch only. **Not pushed; deleted with the worktree at seal** (pressure-test mode: no durable ref lands in the repo).
- `TARGET_TAG_AT_TIP`: `bonsai/v1-on-kilo-7.4.1` — **not created in this pressure-test cycle**. Named for completeness only.
- `PARENT_PIN_ADVANCE_BRANCH`: `pin-advance/kilo-7.4.1` — **not created** (no pin advance in pressure test).
- `VALIDATION_MODE`: `uncommitted-pressure-test` (§1.11)
- Range scope: `BASE_SHA..SOURCE_HEAD_SHA` (exactly the 3 port commits; 0 upstream commits in range)
- Supersession lineage: none (first Kilo cycle; no prior plan superseded)

## Allowlists and Planned Targets (§4.7)

- Runtime allowlist (fork, in the worktree): `packages/opencode/**`, `packages/plugin/**`, `.opencode/opencode.jsonc`.
- Docs allowlist: `.agents/plans/**` (this plan and its validation artifacts), `kilo_context_bonsai/docs/**` (unused here — no evidence commit in a pressure test).
- Planned target paths (union of replay-set rows): `.opencode/opencode.jsonc`, `packages/opencode/src/agent/agent.ts`, `packages/opencode/src/session/compaction.ts`, `packages/opencode/src/session/llm.ts`, `packages/opencode/src/session/overflow.ts`, `packages/opencode/src/session/prompt.ts`, `packages/opencode/test/kilocode/context-bonsai.test.ts`, `packages/plugin/src/index.ts`.
- Explicit non-targets: per the §4.7 slot — this spec; the source branch in place; every other harness repo (`opencode/`, `codex/`, `codex_context_bonsai/`, `pi/`, `pi_context_bonsai/`, `cline/`, `cline_context_bonsai/`, `tweakcc_context_bonsai/`, `gemini-cli/`, `gemini-cli_context_bonsai/`); live `~/.local/share/kilo` state; prior cycles' worktrees; upstream commits beneath the port suffix; `kilo_context_bonsai/` code paths (no row binds them).
- Pre-existing dirty status paths (§1.10, enumerated at generation): parent repo ` M tweakcc_context_bonsai` (and any peer submodule-pin dirt present at execution preflight — re-enumerate then); parent untracked `.agents/pilot/relaunch-run6.sh`, `blog-1-context-bonsai.md`, `blog-2-maintenance.md`; parent modified `docs/agent-specs/forward-port-spec.md` + `docs/agent-specs/derivation-pipeline-spec.md` + `docs/agent-specs/cycle-cadence-ledger.json` + `scripts/detect-pending-target.mjs` + `scripts/detect-pending-target.test.mjs` **only until the Stage 6 landing commit is made** — at execution time these are expected committed; if still dirty, they are the pending owner-tier Stage 6 edit per §1.16's disposition rule and the §1.13 gate-12 hash-assert variant applies. All untouchable by this cycle.

## Classification (§2.2 / §1.14)

Inventory `BASE_SHA..SOURCE_HEAD_SHA`, oldest first — 3 items, 0 unmapped, 0 `manual_review`, 0 merge commits, 0 `already_in_upstream` (the range contains no upstream commits by the `BASE_SHA` ancestry check above):

| sha | subject | bucket | replay_action | target_paths | mapping_type | evidence | rationale | approver |
|---|---|---|---|---|---|---|---|---|
| `7c12284e9c` | `[CB-kilo.1] add Context Bonsai plugin seams (gauge telemetry)` | port_content | cherry-pick `-x` | `.opencode/opencode.jsonc`, `packages/opencode/src/agent/agent.ts`, `packages/opencode/src/session/{compaction,llm,overflow,prompt}.ts`, `packages/opencode/test/kilocode/context-bonsai.test.ts`, `packages/plugin/src/index.ts` | 1:1 | `git show --name-only 7c12284e9c`; subject prefix `[CB-kilo.1]` | Port seam commit: hook telemetry plumbing, seam test, plugin config entry | plan approval (this document) |
| `ab8ca53e9f` | `[CB-kilo.1] fix Context Bonsai plugin path, gauge budget math, and seam regression guard` | port_content | cherry-pick `-x` | `.opencode/opencode.jsonc`, `packages/opencode/src/session/{overflow,prompt}.ts`, `packages/opencode/test/kilocode/context-bonsai.test.ts`, `packages/plugin/src/index.ts` | 1:1 | `git show --name-only ab8ca53e9f` | Port fix commit (Stage 5 defect fixes) | plan approval (this document) |
| `feb4012846` | `[CB-kilo.1] fix stale Context Bonsai plugin path after repo relocation` | port_content | cherry-pick `-x` | `.opencode/opencode.jsonc` | 1:1 | `git show --name-only feb4012846` | One-line plugin-path correction guarded by the seam test | plan approval (this document) |

## Reviewer-Simplicity Evaluation (§2.5 shape analysis)

Three small commits, all `1:1` cherry-picks, no merge commits, no re-implementation candidates. Upstream drift between `BASE_SHA` and `v7.4.1` conflicts rows 1 and 2 (rehearsed 2026-07-06; see Target-API Adaptations below); row 3 applies clean. Conflicts resolve under §2.5's conflict-scope rule (edits confined to the row's `target_paths`, exception record per §1.7 if scope must widen).

## Target-API Adaptations (bound from the §1.15 rehearsal, folded into the replayed commits)

The rehearsal against `v7.4.1` surfaced three drift sites. Each adaptation below is **folded into the named replayed commit at conflict resolution** (not a separate fixup; §1.15/§2.9), keeps the 3-commit count, and stays inside (or shrinks) the row's `target_paths`. The executor resolves each conflict to exactly this binding:

- **Row `7c12284e9c`, `packages/opencode/src/session/llm.ts` — hunk dropped.** Upstream moved the `experimental.chat.system.transform` callsite out of `llm.ts` into `packages/opencode/src/session/llm/request.ts:83`, and the new callsite already passes `{ sessionID: input.sessionID, model: input.model }`. The port's `llm.ts` hunk was documentation-comment-only (no functional content — the functional shape extension lives in `packages/plugin/src/index.ts`, which applies clean). Resolution: take the target side unchanged (`git checkout --ours`); `llm.ts` drops out of the row's realized `target_paths`. Do not edit `llm/request.ts` (out-of-scope path; no marker needed because no edit is made).
- **Row `7c12284e9c`, `packages/opencode/src/session/compaction.ts`.** Upstream restructured the summarize path (prompt building via `buildPrompt`, `selected.head`); the single `experimental.chat.messages.transform` callsite survives. Resolution: target-side file with only that callsite's input changed from `{}` to `{ sessionID: input.sessionID }`, wrapped in the port's `kilocode_change` marker pair (`v7.4.1` has exactly one such callsite in this file; `input.sessionID` remains in scope — the adjacent `experimental.session.compacting` trigger uses it).
- **Row `7c12284e9c`, `packages/opencode/src/session/prompt.ts`.** The main-path trigger moved (now the `experimental.chat.messages.transform` call immediately preceding the `KiloSessionPrompt.injectEditorContext` line inside the step loop; 10-space indent in `v7.4.1`). Resolution: target-side file plus (a) the `overflowTelemetry` import added after the `SessionCompaction` import, (b) that one trigger call replaced by the port's telemetry-bearing block re-indented to the target's depth. `lastFinished`, `step`, `sessionID`, `model` all remain in scope at the site.
- **Row `ab8ca53e9f`, `packages/opencode/src/session/prompt.ts` — hunk shrunk.** `v7.4.1` already imports `Config`, already binds `const config = yield* Config.Service` in the layer, and already provides `Layer.provide(Config.defaultLayer)` — the row's import/service/layer additions are `already_in_upstream` at the target. Resolution: only the telemetry-block edit survives — insert `const cbCfg = yield* config.get()` before the `cbTelemetry` binding, pass `cfg: cbCfg` to `overflowTelemetry`, and delete the `turn: step` line. All other hunks of the row (overflow.ts, plugin/index.ts, seam test, config) apply clean.

Rehearsal evidence (2026-07-06, scratch worktree `kilo/.agent_tmp/rehearsal-v7.4.1`, removed after recording): with these resolutions, `git rev-list --count v7.4.1..HEAD` = 3, `cherry picked from` trailers = 3, realized diff = 7 files (planned set minus `llm.ts`), post-replay seam test 7/7, r01 re-run 57/57 (baseline 57/57), `bun run typecheck` clean, side-repo 67/67 + typecheck clean, config probe shows the replayed plugin entry.

## Acceptance Criteria

1. Baseline rows r01–r05 captured at clean `v7.4.1` (worktree) / current side repo, with r01, r04 green (must-be-green) and r02 `missing-as-expected`.
2. All 3 replay rows cherry-picked in order with `-x` provenance; zero unapproved out-of-scope paths in `git diff --name-status UPSTREAM_HEAD_SHA..HEAD` (working-tree command set, §1.11).
3. Canonical validation set green post-replay (r01 re-run no worse than baseline; seam test 7/7; typecheck; config probe; side-repo rows re-run).
4. E2E gate rows recorded `not-run-in-pressure-test` with the §4.7 required set cited.
5. Worktree, shim symlink, and scratch branch removed at seal (§1.19); no durable ref or commit landed anywhere.
6. Validation Loop Results section records the §1.15 iterations including the target-resolution rehearsal evidence.

## Implementation Phases

**Phase 0 — credentials preflight**: none required (no live e2e in this cycle). Record `skipped: no live e2e in pressure test`.

**Phase 1 — bootstrap and frozen-identity verification** (from parent root):
- `git -C kilo rev-parse feat/context-bonsai-port` == `SOURCE_HEAD_SHA` (else STOP `input-source-drift`).
- `git -C kilo rev-parse v7.4.1` == `UPSTREAM_HEAD_SHA`.
- `git -C kilo merge-base v7.4.1 feat/context-bonsai-port` == `BASE_SHA` (else STOP `input-base-sha-mismatch`).
- Toolchain: `bun --version`, `jq --version`, `sha256sum --version` (sqlite3 not needed — no live e2e).
- §1.10 clean-state: `git status --short` in parent, `kilo/`, `kilo_context_bonsai/` — only enumerated pre-existing dirty paths.
- Spec immutability pre-hash: `sha256sum docs/agent-specs/forward-port-spec.md` (recorded for gate 12).

**Phase 2 — workspace preparation** (from `kilo/`):
- `git worktree add .agent_tmp/rebase-on-v7.4.1 -b replay/context-bonsai-on-kilo-7.4.1 v7.4.1` (STOP `input-cycle-already-generated` if the worktree or branch already exists).
- Hydration (worktree root): `test -d node_modules || bun install`. Expect a floating `ghostty-web` lockfile pin change — restore with `git checkout -- bun.lock` after hydration and after baseline, before replay (§4.7 Toolchain).
- Script existence preflight (worktree): `jq -r '.scripts.typecheck, .scripts.test' packages/opencode/package.json` non-empty.

**Phase 3 — baseline capture** (clean `v7.4.1`):
- r01 (must-be-green), worktree `packages/opencode`: `bun test test/tool/registry.test.ts test/session/message-v2.test.ts test/session/session.test.ts` (fail → STOP `env-upstream-regression`).
- r02, worktree root: `test ! -f packages/opencode/test/kilocode/context-bonsai.test.ts && echo missing-as-expected || (echo unexpected-presence && false)`.
- r03, worktree `packages/opencode`: `bun run typecheck` (recorded; informational).
- r04 (must-be-green), `kilo_context_bonsai/`: `bun install && bun test`; r05: `bun run typecheck`.
- Restore build-generated lock churn: worktree root `git checkout -- bun.lock` (and `git status --short` must then show clean).

**Phase 4 — replay** (worktree root): `git cherry-pick -x 7c12284e9c76bd5bf9f4aeac50076e2714ab7885 ab8ca53e9fbbd9342d1e2b962850d38eb494c00e feb40128460987971f9b98dc34fff515226ff83c`. Rows 1 and 2 conflict at the target; resolve each conflict to exactly the Target-API Adaptations bindings above (rehearsed green). Any conflict outside those bound sites: resolve within the row's `target_paths` per §2.5; wider edits need a §1.7 exception record before landing. Verify count and provenance: `git rev-list --count v7.4.1..HEAD` == 3; `git log --format=%B v7.4.1..HEAD | grep -c 'cherry picked from'` == 3.

**Phase 5 — post-replay validation** (canonical set, §4.7):
- Create the shim: from `kilo/.agent_tmp/`: `ln -s ../../kilo_context_bonsai kilo_context_bonsai` (skip if present from this run).
- Worktree root: `test -d node_modules || bun install`; config probe `grep -A1 '"plugin"' .opencode/opencode.jsonc` shows the replayed plugin entry.
- Worktree `packages/opencode`: r01 command re-run (no worse than baseline); `bun test test/kilocode/context-bonsai.test.ts` (7/7 must pass); `bun run typecheck` (no worse than r03 baseline).
- `kilo_context_bonsai/`: r04 `bun test`, r05 `bun run typecheck` re-run.
- Scope check (working-tree mode): `git diff --name-status v7.4.1..HEAD` ⊆ planned target paths.

**Phase 6 — e2e gate**: rows for the §4.7 required set (live tool registration, E2E-01, E2E-02, E2E-03, E2E-06, E2E-07/Protocol A, plus open E2E-04), each recorded `not-run-in-pressure-test`, citing procedure `kilo_context_bonsai/docs/e2e-testing.md` and command bindings `docs/kilo-e2e-runbook.md`. No exception record needed: gate 11 applies to sealing a release, and this pressure test releases nothing — the §1.11 mode note and this plan's Non-Goal record the scope.

**Phase 7 — seal (pressure-test variant) and cleanup (§1.19)**:
- Completion checklist (below) asserted.
- Record final evidence: `git -C kilo/.agent_tmp/rebase-on-v7.4.1 log --oneline v7.4.1..HEAD`; `git diff --stat v7.4.1..HEAD`.
- Remove scratch, in order, from `kilo/`: `rm .agent_tmp/kilo_context_bonsai` (symlink); `git worktree remove --force .agent_tmp/rebase-on-v7.4.1`; `git branch -D replay/context-bonsai-on-kilo-7.4.1`; `git worktree prune`. STOP path: on any STOP, scratch is preserved as run evidence until dispositioned (§1.19), then removed by the disposition act.
- Spec immutability (gate 12): `test -f docs/agent-specs/forward-port-spec.md` and hash equal to the Phase 1 recording (dirty-enumerated variant if the Stage 6 edit is still uncommitted).

## Validation Commands by Working Directory

- Worktree root (`kilo/.agent_tmp/rebase-on-v7.4.1`): `bun install` (conditional), `git checkout -- bun.lock`, r02 probe, cherry-picks, `grep -A1 '"plugin"' .opencode/opencode.jsonc`, diff/scope commands.
- Worktree `packages/opencode`: r01 test row, seam test, `bun run typecheck`.
- `kilo_context_bonsai/`: `bun install`, `bun test`, `bun run typecheck`.
- Parent root: frozen-identity and status commands (Phase 1), spec-immutability commands.

## E2E Gate Section

Procedure: `kilo_context_bonsai/docs/e2e-testing.md`. Command binding: `docs/kilo-e2e-runbook.md`. Required minimum per §4.7: live tool registration, E2E-01, E2E-02, E2E-03, E2E-06, E2E-07/Protocol A (all EXECUTED 2026-07-06 against the derivation-time build) plus E2E-04 for the first cycle that reaches the live gate. This pressure test does not reach the live gate (Non-Goal); all rows `not-run-in-pressure-test`.

## Worktree Artifact Check (§1.14.10)

At generation (2026-07-06): `kilo/.agent_tmp/` does not exist; no planned target path is `tracked-dirty` or `existing-untracked` in `kilo/` (`git -C kilo status --short` clean). The executor re-runs this check immediately before Phase 2.

## Plan Approval and Commit Status

- Approval status: **approved** (owner tier, Stage 6 acceptance run, 2026-07-06). Landing authorization: the 2026-07-05 standing order — local landings are routine; this pressure test lands nothing regardless.
- Approval citation: this document plus the Stage 6 unit's hand-off record.
- Plan commit hash: recorded at the Stage 6 landing commit (this plan is committed with the binding).
- Ready-for-orchestration: **not applicable as an executor launch** — this cycle's §1.15 rehearsal (below) is the acceptance-gate execution; a real v7.4.1 forward-port cycle regenerates under §1.20 cadence.

## Validation Loop Results (§1.15)

**Iteration 1 (2026-07-06).**
- *Target-resolution rehearsal* (disposable scratch worktree `kilo/.agent_tmp/rehearsal-v7.4.1` at `UPSTREAM_HEAD_SHA`, hydrated per §1.12, removed after recording): baseline r01 57/57 green, r02 `missing-as-expected`, side-repo r04 67/67 + r05 typecheck green at clean `v7.4.1`. Replay: row `7c12284e9c` conflicted in `compaction.ts`, `llm.ts`, `prompt.ts`; row `ab8ca53e9f` conflicted in `prompt.ts`; row `feb4012846` applied clean. **Generation-blocking finding**: source-era API drift at the target (system-transform callsite moved to `session/llm/request.ts`; compaction/prompt paths restructured; Config service already present upstream). **Fix**: the four target-API adaptations bound in the Target-API Adaptations section, folded into the replayed commits at conflict resolution — re-rehearsed green: 3/3 commits with `-x` provenance, realized diff 7 files ⊆ planned targets, post-replay seam test 7/7, r01 57/57 (no worse than baseline), `bun run typecheck` clean, side-repo rows green, config probe green.
- *Missing-details review* (independent, repository-inspecting): 1 blocking finding — Validation Loop Results section empty at review time (this section closes it). All frozen SHAs, inventory file lists, referenced docs, toolchain, and slot-table conformance verified clean. Non-blocking: parent repo is on branch `spec/update-cadence-rule` (plan assumes no particular parent branch — executor preflight should not assume `main`); the dirty-path enumeration's "expected committed by execution" note is an assumption the executor re-checks at preflight (the §1.13 gate-12 dirty-enumerated variant applies while uncommitted).
- *Ambiguity review* (independent, repository-inspecting): 1 blocking finding — the rehearsal worktree `kilo/.agent_tmp/rehearsal-v7.4.1` was still on disk at review time, contradicting the Worktree Artifact Check and inviting executor divergence (STOP vs silent delete). **Fix**: the rehearsal scratch was removed per §1.15/§1.19 immediately after its evidence was recorded (`git worktree remove --force` + `git worktree prune`; `kilo/.agent_tmp/` no longer exists and `git -C kilo status --short` is clean). The Worktree Artifact Check statement is true as of plan finalization; the executor re-runs the check before Phase 2 regardless, and if any `kilo/.agent_tmp/` content is found then, it is a non-enumerated dirty path — STOP `env-workspace-dirty` for invoker disposition, never a silent delete.

**Iteration 2 (2026-07-06).** Both reviewers re-checked the amended plan (Target-API Adaptations section, this results section, rehearsal-scratch removal): zero blocking findings. Loop converged; stopped per §1.15's early-stop rule.

## Completion Checklist (§1.13, pressure-test realization)

1. 3/3 inventory items mapped — yes (Classification).
2. 0 `manual_review` rows — yes.
3. 0 late fixes — assert at seal.
4. Replay-set = the Classification table (in-plan, per §1.14.4 git-fork form); no separate checksum artifact in pressure-test mode.
5. n/a (no separate approval artifacts).
6. Baseline rows complete with provenance — assert at seal.
7. Replay verification by `-x` provenance + count — assert at seal.
8. Post-replay no worse than baseline; must-be-green rows green — assert at seal.
9. Scope check via working-tree diff — assert at seal.
10. 0 exception records outstanding — assert at seal.
11. E2E rows `not-run-in-pressure-test` per plan scope — recorded.
12. Spec immutability hash — assert at seal.
13. Reviewer/judge approvals: §1.15 loop reviewers (this generation run) recorded below.
14. Release-gate steps: out of scope (pressure test; no release).
