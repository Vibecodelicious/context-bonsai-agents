# Story: Forward-port cycle — Codex Context Bonsai onto upstream release `rust-v0.125.0`

Routine forward-port cycle per `docs/agent-specs/forward-port-spec.md` (Part 1 core, Part 2 shape binding — git-fork, §4.5 Codex slot table). Generated by the owner tier on 2026-07-06 from the spec plus the §4.5 slots. This is the **first executed routine cycle for the Codex harness**: the Stage 6 acceptance plan (`story-rebase-cycle-5a680bcb3a5d63dffd7b77a4cdd414b2c951ab9c.md`) proved the binding as an uncommitted pressure test against the port's own base; this plan is `committed-final` against a real pending release. All relative paths are from the parent repo root (`context-bonsai-agents/`); worktree/side-repo commands state their working directory.

**Artifact keying (§1.14 same-source different-target rule)**: the closed Stage 6 pressure-test cycle already used this `SOURCE_HEAD_SHA` against a different target (the raw base commit `1fda843fbc`), occupying `.agents/plans/story-rebase-cycle-5a680bcb3a….md` and `replay-set-5a680bcb3a….json`. This cycle's plan and every `<SOURCE_HEAD_SHA>`-keyed validation artifact therefore carry the `-on-rust-v0.125.0` suffix. The prior cycle's artifacts are untouchable history.

## Goal

Replay the Codex Context Bonsai port — the contiguous 14-commit `[CB-codex.1]`/`[Story CO1]`/`[Story CO2]` suffix tipped at `SOURCE_HEAD_SHA=5a680bcb3a5d63dffd7b77a4cdd414b2c951ab9c` on `feat/spec-compliance` — onto the frozen upstream release tag `rust-v0.125.0` in an isolated worktree, pass the canonical validation set and the live e2e gate (including the port's first live E2E-04 and E2E-06), tag the rebase point, and advance the parent pin locally. Pushes (§2.9 steps 5–6) are owner-gated and excluded from this plan's executable scope.

## Non-Goal

- No code changes to `codex_context_bonsai/` (no replay-set row binds any; only the e2e results doc named under Evidence paths may land there per §4.5).
- No replay/rewrite of the source branch in place; all replay happens in the scratch worktree.
- No targeting of any later release (`rust-v0.126.0`…`rust-v0.142.5`): the invoker-selected target is `rust-v0.125.0` (see Frozen Inputs rationale). Later releases are later cycles.
- No pushes: §2.9 steps 5–6 execute only as their own owner-approved continuation. The cycle is not complete until that ladder completes.
- Generation does not create the cycle worktree, build, or commit anything in the fork; the executor performs those on execution. (The §1.15 rehearsal used its own disposable worktree, removed at loop close.)

## Execution Outcome Statement

After execution, the worktree's `HEAD` is based on `UPSTREAM_HEAD_SHA` (`637f7dd6d7`, = `rust-v0.125.0`) and contains exactly the 14 replayed commits from the approved replay set in topological order — verified by cherry-pick provenance trailers, never by source-SHA equality. The canonical validation set and the required live e2e scenario set are green; the local tag `bonsai/v1-on-codex-0.125.0` exists at the replayed tip; the parent pin is advanced on `pin-advance/codex-0.125.0`; nothing is pushed.

## Frozen Inputs (§1.1)

Invoker-supplied, frozen at cycle start; never re-resolved. All commands reference the frozen values below.

- `SOURCE_REF`: `refs/heads/feat/spec-compliance`
- `SOURCE_HEAD_SHA`: `5a680bcb3a5d63dffd7b77a4cdd414b2c951ab9c`
- `UPSTREAM_REF`: `refs/tags/rust-v0.125.0` (fetched per §2.1's explicit tag fetch — the upstream remote's refspec is heads-only)
- `UPSTREAM_HEAD_SHA`: `637f7dd6d737f3961e6bf32fbb3861c4953269c5` (`git rev-parse --verify 'refs/tags/rust-v0.125.0^{commit}'`)
- **Target-selection rationale**: `scripts/detect-pending-target.mjs --harness Codex` reports pending (ported `0.0.2504301132`, upstream newest `0.142.5`). The detector's signal is a detection, not target selection; the invoker selects. Version-sorted "smallest pending" is misleading here: the datever `rust-v0.0.25xxx` tags sort below every semver tag but date from April–May 2025, a year **behind** the port's base commit (`1fda843fbc`, upstream `main` 2026-04-23) — replaying onto them would move the port backward onto stale history. The invoker therefore selects the smallest stable release tag that **contains the port's base**: `rust-v0.125.0` (2026-04-24; verified `git merge-base --is-ancestor 1fda843fbc rust-v0.125.0`; `rust-v0.124.0` does not contain the base). This is an invoker choice recorded here, not a spec default.
- `BASE_SHA`: `1fda843fbcd825d92f41005060a181ebda615ff7` — **computed, not asserted** (§2.1): `git merge-base 637f7dd6d7 5a680bcb3a` (resolved after the shallow-checkout deepen; §4.5). The port suffix sits on upstream `main` at this commit; `rust-v0.125.0` contains it, so the inventory range `UPSTREAM_HEAD_SHA..SOURCE_HEAD_SHA` holds exactly the 14 port commits and no upstream history. 48 upstream commits lie between `BASE_SHA` and the tag (`git rev-list --count 1fda843fbc..637f7dd6d7`) — the drift the replay crosses; two known conflicts from it are bound in Phase 4.
- `TARGET_WORKTREE`: `/home/basil/projects/context-bonsai-agents/codex/.agent_tmp/rebase-on-rust-v0.125.0`
  - **Path-dependency symlink shim** (§4.5 Naming): before any build in the worktree, ensure `codex/.agent_tmp/codex_context_bonsai` → `../../codex_context_bonsai` exists — the fork's `codex-rs/core/Cargo.toml` declares `codex-context-bonsai = { path = "../../../codex_context_bonsai" }`, which from `.agent_tmp/rebase-on-…/codex-rs/core` resolves into `.agent_tmp/`. Step-scoped scratch, removed with the worktree per §1.19. (Clean-upstream baseline rows need no shim — upstream has no path dependency — but the shim already exists and is harmless at baseline.)
- `TARGET_BRANCH`: `replay/context-bonsai-on-codex-0.125.0`
- `TARGET_TAG_AT_TIP`: `bonsai/v1-on-codex-0.125.0` (created locally at §2.9 step 1; **not pushed** in this plan's scope)
- `PARENT_PIN_ADVANCE_BRANCH`: `pin-advance/codex-0.125.0` (§2.9 step 3; local only, created from the parent's current working-line tip)
- `VALIDATION_MODE`: `committed-final` (§1.11 default; a real release is the target)
- Range scope: `UPSTREAM_HEAD_SHA..SOURCE_HEAD_SHA` (default; no author-filter override)
- Upstream commits in range: 0 (the range is exactly the 14 port commits; `git cherry` marks all 14 `+`)
- Supersession lineage: none. The Stage 6 pressure-test cycle at this `SOURCE_HEAD_SHA` closed sealed; it is a distinct prior cycle, not a superseded plan.

## Allowlists and Planned Targets (§1.14 item 3)

Per §4.5, no overrides for this cycle:

- **Runtime allowlist**: fork paths `codex-rs/core/**`, `codex-rs/tools/**`, `codex-rs/hooks/**`, `codex-rs/protocol/**`, `codex-rs/rollout/**`, `codex-rs/app-server-protocol/**`, `codex-rs/Cargo.lock`; side-crate paths `codex_context_bonsai/{src/**,tests/**,Cargo.toml,Cargo.lock}` (no row binds any this cycle).
- **Docs allowlist**: `.agents/plans/**`, `codex_context_bonsai/docs/**`.
- **State-only**: metadata/state artifacts outside both.
- **Planned worktree targets (replay only, not direct edits)**: the union of replay-set `target_paths` — all inside the runtime allowlist.
- **Planned parent-repo targets (generator writes)**: this plan file; `.agents/plans/validation/replay-set-5a680bcb3a5d63dffd7b77a4cdd414b2c951ab9c-on-rust-v0.125.0.json`; rehearsal evidence logs `.agents/plans/validation/rehearsal-codex-on-rust-v0.125.0.log` and `.agents/plans/validation/rehearsal-codex-sidecrate-on-rust-v0.125.0.log`. **Executor writes (parent repo)**: `.agents/plans/validation/baseline-5a680bcb3a5d63dffd7b77a4cdd414b2c951ab9c-on-rust-v0.125.0.json`; `.agents/plans/validation/exceptions-5a680bcb3a5d63dffd7b77a4cdd414b2c951ab9c-on-rust-v0.125.0.json` (empty by default); the §2.9 step-3 pin-advance commit on `pin-advance/codex-0.125.0`. **Executor writes (side crate, e2e gate)**: `codex_context_bonsai/docs/e2e-results-<DATE>-live.md` (if the UTC `<DATE>` collides with an existing committed record, disambiguate as `e2e-results-<DATE>-codex-0.125.0-live.md` and leave the prior record untouched — the cline-2.17.0 naming-collision precedent, §4.6 Evidence paths).
- **Explicit non-targets** (§4.5): the spec (`docs/agent-specs/forward-port-spec.md`); the source branch `feat/spec-compliance` in place; `opencode/`, `cline/`, `cline_context_bonsai/`, `pi/`, `pi_context_bonsai/`, `tweakcc_context_bonsai/`, and every other harness repo; `~/.codex/**` auth and live user sessions (rollout files read for evidence only, never edited/committed); prior cycles' worktrees and the Stage 6 cycle's artifacts; the upstream commits below the port suffix.

## Source Commit Classification (§1.14 item 4)

Inventory produced by the §2.2 command sequence (run read-only at generation against `codex/`, range `637f7dd6d7..5a680bcb3a`): 14 commits, topological order, **zero merges** (`git log --merges` empty), all 14 marked `+` by `git cherry -v` (none patch-equivalent in upstream). Bucket counts: `required_runtime` 14; every other bucket 0. No hard-blocking rows; the >50% `manual_review` structural threshold (§1.17) is not approached. The rows are identical in membership and order to the Stage 6 cycle's classification (same source suffix, same base ancestor); full per-row `target_paths` are in the committed replay-set artifact.

| source_sha | subject | target_paths (summary) |
|---|---|---|
| `5cad90f9e70d12c4f760625acd09c071f0505e10` | [CB-codex.1] extend CompactedItem with optional bonsai archive payload | `codex-rs/protocol/{Cargo.toml,src/protocol.rs}` |
| `26e6abb721a5f2d46eaca4cc59a5ac92216c5136` | [CB-codex.1] add codex-core context_bonsai wrapper module | `codex-rs/Cargo.lock`, `codex-rs/core/**` (wrapper module, tests, Cargo.toml), `codex-rs/app-server-protocol/**`, `codex-rs/rollout/**` |
| `d94efcf17e289cb189b2bd2b6154f248e5f5fe41` | [CB-codex.1] harden context_bonsai wrapper guards and anchor ids | `codex-rs/core/src/context_bonsai{,_tests}.rs` |
| `96bcf61ef68abd5aaa9aaa4fa69f4b3851c7484e` | [CB-codex.1] wire bonsai prune/retrieve through Session + SessionState | `codex-rs/core/src/session/**`, `codex-rs/core/src/state/session.rs` |
| `a17bc74e7398cf4a72ecf77ebbd774c60536eb4d` | [CB-codex.1] register context-bonsai prune/retrieve in the tool registry | `codex-rs/core/src/tools/**`, `codex-rs/tools/src/**` |
| `c9e69c0f392b66e16071b20b6c65bedf6a6aa586` | [CB-codex.1] cover bonsai archive payload through rollout reconstruction | `codex-rs/core/src/session/rollout_reconstruction_tests.rs` |
| `fd56b70e1728f86a27ee13e9213270a142be44c5` | [CB-codex.1] inject bonsai guidance + gauge context through hook events | `codex-rs/Cargo.lock`, `codex-rs/core/src/hook_runtime.rs`, `codex-rs/hooks/**` |
| `6b2f5a435b12f1bb6ed134426f8562b42c4755b8` | [CB-codex.1] adjust compact tests to account for bonsai guidance row | `codex-rs/core/tests/**` |
| `1beb7b2d9d874f4200f5bf537484b5124b79001f` | [CB-codex.1] Fix clippy regressions in context_bonsai module | `codex-rs/core/src/context_bonsai{,_tests}.rs` |
| `689c4e1fc2117da22fade27c6ee33bdc20dc211c` | [CB-codex.1] Advance bonsai_turn_seq at user-turn boundary | `codex-rs/core/src/session/{mod.rs,tests.rs}` |
| `1863ca285a3406220537ae0dac5307aa91cea674` | [CB-codex.1] Skip bonsai guidance prepend on session resume | `codex-rs/hooks/src/events/session_start.rs` |
| `8e62af863edc1458509ff128611d1212b68ea85a` | [CB-codex.1] Rehydrate bonsai archive cache on resume | `codex-rs/core/src/session/{mod.rs,rollout_reconstruction_tests.rs}` |
| `78af9df233fdbf5896a873ff4ff273160423b7cb` | [Story CO1] Project all model-visible ResponseItem variants in extract_text | `codex-rs/core/src/context_bonsai{,_tests}.rs` |
| `5a680bcb3a5d63dffd7b77a4cdd414b2c951ab9c` | [Story CO2] Populate is_prune_wrapper flag on Codex projection | `codex-rs/core/src/context_bonsai{,_tests}.rs` |

Merge-commit scan: `git log --merges 637f7dd6d7..5a680bcb3a` empty; no merge rows.

## Reviewer-Simplicity Evaluation (§1.14 item 5 / §2.5)

All 14 rows are `cherry-pick`. The target tag sits 48 upstream commits past the port's base on the same `main` line; the §1.15 rehearsal (below) replayed all 14 rows against it with exactly **two conflicts**, both single-hunk and resolved inside their rows' `target_paths` (bound in Phase 4). For each row `cherry-pick` (with the bound conflict resolutions) is preferred over `re-implement`. The §2.5 re-implement escape is available only if a cherry-pick conflicts irresolvably inside the row's `target_paths` — then STOP, record the reviewer-simplicity evaluation and behavioral-contract table, and obtain reviewer+judge approval before switching.

## Re-Implementation Behavioral Contract (§2.5)

Not applicable (all rows `cherry-pick`). If a row is forced to `re-implement`, populate this section with the §2.5 fields before execution proceeds.

## Validation Mode (§1.11)

Mode: `committed-final`. Replay commits land on `TARGET_BRANCH` in the isolated worktree; final verification uses commit-range commands against `637f7dd6d7..HEAD`; the §2.9 ladder runs through step 4 (tag, e2e gate, local pin advance, pre-publish install gate), stopping before the push steps per Non-Goals.

## Acceptance Criteria (§1.14 item 6)

- [ ] All replay work happens only in `TARGET_WORKTREE`; the executor never edits `codex/` on `feat/spec-compliance` in place and never touches any explicit non-target.
- [ ] `TARGET_BRANCH` is created from `UPSTREAM_HEAD_SHA` (`637f7dd6d7`) exactly; `git merge-base 637f7dd6d7 HEAD` returns `637f7dd6d7`.
- [ ] Replayed inventory in `637f7dd6d7..HEAD` is exactly 14 commits in topological order matching the replay-set rows, identified by cherry-pick provenance trailers. Source-SHA equality is forbidden as evidence (§2.6).
- [ ] Replay-set artifact present, schema-valid (§2.4 field order), 14 rows; checksum (below) verified before replay and unchanged at seal.
- [ ] Baseline artifact created before any replay commit; all rows carry non-null `provenance_ref`; must-be-green rows green; `r02` existence probe = `missing-as-expected`; `r04` (side-crate `cargo fmt --check`) recorded informational per §4.5 — its known pre-existing 233-line drift at the committed side-crate HEAD is baseline data, not a failure.
- [ ] Exception ledger exists (empty or with approved rows); no unresolved exceptions at seal.
- [ ] Zero `manual_review` rows (classification: all 14 `required_runtime`).
- [ ] Post-replay canonical validation set green; no net-new failures vs baseline; must-be-green rows green.
- [ ] Change-scope: worktree `git diff --name-status 637f7dd6d7..HEAD` lists only paths in the union of replay-set `target_paths`; generated-artifact exclusion probe returns 0 (§2.7). (`codex-rs/Cargo.lock` rides rows `26e6abb7`/`fd56b70e` as committed port content per the §4.5 allowlist; it is not a §2.7 generated-artifact class.)
- [ ] E2E release gate (§1.13 gate 11): the §4.5 required scenario set green against the freshly built worktree binary — live registration, E2E-01, E2E-02, E2E-03, E2E-07/Protocol A, **and the first live E2E-04 (gauge) and E2E-06 (persistence across resume)** — driven per `docs/codex-e2e-runbook.md`; results doc committed in the side crate.
- [ ] §2.9 steps 1–4 completed: local tag `bonsai/v1-on-codex-0.125.0`; e2e behavioral gate; local parent pin advance on `pin-advance/codex-0.125.0`; pre-publish install gate PASS (bundle-sourced, on real disk — never `/tmp`). Steps 5–6 not executed (owner-gated).
- [ ] Spec-immutability (§1.13 gate 12): `test -f docs/agent-specs/forward-port-spec.md && test -z "$(git diff --name-only -- docs/agent-specs/forward-port-spec.md)"` — or, if the spec is invoker-enumerated dirty at execution time, the §1.10 hash re-assertion form.
- [ ] Reviewer+judge approvals recorded (Approval section; standing-order routine landing).
- [ ] §1.19 cleanup rows executed on the run's realized path (seal or STOP).

## Implementation Phases (§1.14 item 7)

Working-directory shorthands: `PARENT` = `/home/basil/projects/context-bonsai-agents`; `CODEX` = `$PARENT/codex`; `WORKTREE` = `$PARENT/codex/.agent_tmp/rebase-on-rust-v0.125.0`; `SIDE` = `$PARENT/codex_context_bonsai`.

### Phase 0: Credentials preflight (§4.5 Credentials)

Live e2e is in scope. Presence probe (exit status only): `codex login status` — exit 0 means Codex's own sign-in (ChatGPT account or API key) is provisioned on this rig (verified exit 0 at generation, 2026-07-06). Run records name the provider only — never keys or auth files. Credential absence at Phase 6 records the affected scenarios `BLOCKED` (`input-credentials-missing` if the seal cannot pass) — never a plan-wide preflight hard-fail; a failed behavioral oracle is never `BLOCKED`.

### Phase 1: Preflight — toolchain, clean state, frozen identities, shallow-deepen (§1.10, §1.12, §2.1, §4.5)

1. Toolchain presence (§4.5): `cargo --version`; `rustup show` (fork and side crate both pin `channel = "1.93.0"` via `rust-toolchain.toml`; cargo resolves per directory); `command -v jq`; `command -v sha256sum`. Bootstrap beyond toolchain: none — the first worktree build fetches crates itself.
2. Clean-state check (§1.10). From `PARENT`: `git status --short` must contain **only** invoker-enumerated pre-existing dirty paths. Enumerated for this cycle, exactly: submodule-pin lines of the form ` M <submodule>` (mode ` M` on a path `git submodule status` lists — routinely dirty from local-only bookkeeping). **No other path is enumerated**: the invoker commits this plan, the replay set, the two rehearsal logs, and the runbook's pre-publish sourcing block in the plan-commit series **before launching the executor** (the Plan Commit Hash item below is the executable precondition — the executor never commits them itself). If any of those paths appear dirty/untracked at execution preflight, that is an invoker error: STOP `env-workspace-dirty`; do not absorb them. Any dirty path that is not a submodule-pin line fails preflight (`env-workspace-dirty`); record the observed pin set in the intent log. Also record `sha256sum docs/agent-specs/forward-port-spec.md` here and re-assert the identical value at seal gate 12.
3. Shallow-deepen (§4.5): from `CODEX`, `git rev-parse --is-shallow-repository` (currently `true`). A merge-base failure ("no common ancestor") on a shallow checkout is an `env-*` condition, not divergence: deepen from `upstream` (`git fetch --deepen=<n> upstream` or targeted tag fetches) until step 5's merge-base resolves. At generation the tag was fetched with sufficient depth; the recompute already resolves.
4. Upstream tag freeze (§2.1): from `CODEX`, `git fetch upstream "refs/tags/rust-v0.125.0:refs/tags/rust-v0.125.0"` (idempotent if present); `test "$(git rev-parse --verify 'refs/tags/rust-v0.125.0^{commit}')" = "637f7dd6d737f3961e6bf32fbb3861c4953269c5"`. (Do not run `git fetch --all --prune` against this shallow submodule — the targeted tag fetch is the §2.1 freeze realization here; §4.5's heads-only refspec note.)
5. Frozen-identity verification: `git rev-parse --verify refs/heads/feat/spec-compliance` → `5a680bcb3a5d63dffd7b77a4cdd414b2c951ab9c` (differing → `input-source-drift`, fresh plan per §1.9; never reset the branch). `BASE_SHA` recompute: `git merge-base 637f7dd6d737f3961e6bf32fbb3861c4953269c5 5a680bcb3a5d63dffd7b77a4cdd414b2c951ab9c` → `1fda843fbcd825d92f41005060a181ebda615ff7`.
6. Replay-set checksum verify (§1.4). From `PARENT`:
   `test "$(jq -c < .agents/plans/validation/replay-set-5a680bcb3a5d63dffd7b77a4cdd414b2c951ab9c-on-rust-v0.125.0.json | sha256sum | cut -d' ' -f1)" = "2fe527f22887571bbe1db92274fccd94de895df5f14155acb4d064082749b651"`
7. Create the empty exception ledger if absent: `.agents/plans/validation/exceptions-5a680bcb3a5d63dffd7b77a4cdd414b2c951ab9c-on-rust-v0.125.0.json` = `[]`.

### Phase 2: Isolated worktree creation + path-dependency symlink shim (§2.5, §4.5 Naming)

8. From `CODEX`: `git worktree add -B replay/context-bonsai-on-codex-0.125.0 /home/basil/projects/context-bonsai-agents/codex/.agent_tmp/rebase-on-rust-v0.125.0 637f7dd6d737f3961e6bf32fbb3861c4953269c5`
9. In `WORKTREE`: `test "$(git rev-parse HEAD)" = "637f7dd6d737f3961e6bf32fbb3861c4953269c5"`.
10. **Shim** (before any build): `ln -sfn ../../codex_context_bonsai /home/basil/projects/context-bonsai-agents/codex/.agent_tmp/codex_context_bonsai`; verify `readlink -f` resolves to `SIDE`. If the path already exists as the identical symlink (left by prior cycles/rehearsals), verify and reuse — not a collision.

### Phase 3: Baseline capture (clean upstream tag; no replay yet) (§1.6, §2.8, §4.5)

11. Create `.agents/plans/validation/baseline-5a680bcb3a5d63dffd7b77a4cdd414b2c951ab9c-on-rust-v0.125.0.json`. Row schema (field order §1.6): `row_id`, `command`, `frozen_upstream_head_sha`, `frozen_source_head_sha`, `exit_code`, `result`, `artifact_path`, `provenance_ref`. Missing required fields hard-fail; `BLOCKED` only for genuinely unavailable external dependencies; no `n/a` placeholders. Rows (§4.5 baseline set):
    - `r01` (**must-be-green**), from `WORKTREE/codex-rs`: `cargo build --bin codex` (memory-constrained rigs: `CARGO_INCREMENTAL=0 CARGO_PROFILE_DEV_DEBUG=0 cargo build --bin codex`; the generation rehearsal needed no accommodation on this rig).
    - `r02`, from `WORKTREE` root: `test ! -f codex-rs/core/src/context_bonsai.rs && echo missing-as-expected || (echo unexpected-presence && false)`. `provenance_ref`: `git ls-files codex-rs/core/src/context_bonsai.rs` (empty on the tag).
    - `r03` (**must-be-green**), from `SIDE` (the real side repo — not part of the worktree): `cargo test`.
    - `r04` (informational, not must-be-green), from `SIDE`: `cargo fmt --check` — fails at the committed side-crate HEAD under the pinned 1.93.0 toolchain (233-line pre-existing drift, §4.5); record the result; only *new* drift introduced by this cycle is a failure.
    - `r05` (**must-be-green**), from `SIDE`: `cargo clippy --all-targets --all-features`.
12. Must-be-green failure on the clean upstream tag → STOP + escalate (`env-upstream-regression`; a pre-existing upstream regression is not this cycle's to fix).

### Phase 4: Replay — cherry-pick in topological order, two bound conflicts (§2.5)

Conflict scope: each row's conflict-resolution edits must stay inside that row's `target_paths`. An edit outside that scope is an out-of-scope fixup → STOP, add an exceptions-ledger row citing the authorizing replay-set row + reviewer/judge approval refs, then continue.

13. Cherry-pick the 14 rows **in replay-set order**, `git cherry-pick -x <source_sha>`, in order: `5cad90f9`, `26e6abb7`, `d94efcf1`, `96bcf61e`, `a17bc74e`, `c9e69c0f`, `fd56b70e`, `6b2f5a43`, `1beb7b2d`, `689c4e1f`, `1863ca28`, `8e62af86`, `78af9df2`, `5a680bcb`.
14. **Bound conflict #1 — row `96bcf61e`, `codex-rs/core/src/session/session.rs`** (rehearsal-verified): upstream 0.125.0 restructured the `Session::new` region (thread-persistence refactor), so the port's one-line constructor addition conflicts. Resolution, entirely inside the row's `target_paths`: keep the upstream (`HEAD`/"ours") side of the conflicted hunk in full — discard the "theirs" copy of the old constructor region — then insert the single line `bonsai_turn_seq: AtomicU64::new(0),` immediately after the line `next_internal_sub_id: AtomicU64::new(0),` inside the `let sess = Arc::new(Session {…})` literal (exactly one occurrence in the file). The struct-field declaration hunk (`pub(super) bonsai_turn_seq: AtomicU64,`) auto-merges. Verify before continuing: `grep -c 'bonsai_turn_seq: AtomicU64::new(0),' codex-rs/core/src/session/session.rs` → `1`, and `grep -c 'bonsai_turn_seq' codex-rs/core/src/session/session.rs` → `2` (declaration + constructor). Then `git add` and `git cherry-pick --continue`. Never resolve with `git checkout --ours <file>` — it discards the port's auto-merged hunks in the same file.
15. **Bound conflict #2 — row `a17bc74e`, `codex-rs/core/src/tools/handlers/mod.rs`** (rehearsal-verified): a one-hunk import collision — upstream renamed `codex_protocol::models::PermissionProfile` to `AdditionalPermissionProfile` on the line the port's two `pub use context_bonsai::…` lines sit beside. Resolution: keep upstream's `use codex_protocol::models::AdditionalPermissionProfile;` and the port's two lines `pub use context_bonsai::ContextBonsaiPruneHandler;` / `pub use context_bonsai::ContextBonsaiRetrieveHandler;` above it (drop the port-era `PermissionProfile` import line). Then `git add` and `git cherry-pick --continue`.
16. Any conflict beyond these two: resolve within the row's `target_paths` per §2.5 if mechanically evident from the two sides; otherwise STOP per §2.5's escape ladder. Provenance-trailer count (§2.6; run identically here and at seal), from `WORKTREE`:
    `test "$(git log --format='%b' 637f7dd6d737f3961e6bf32fbb3861c4953269c5..HEAD | grep -cE '^\(cherry picked from commit [0-9a-f]{40}\)$')" = "14"`

### Phase 5: Post-replay canonical validation set (§4.5)

17. Ensure the shim exists (Phase 2 step 10), then:
    - From `WORKTREE/codex-rs`: `cargo build --bin codex`
    - From `WORKTREE/codex-rs`: `cargo test -p codex-core context_bonsai` (replayed module, tool-handler, and reconstruction tests)
    - From `SIDE`: `cargo test` (r03), `cargo fmt --check` (r04, informational), `cargo clippy --all-targets --all-features` (r05)
    - Offline registration probe, from `WORKTREE`: `strings codex-rs/target/debug/codex | grep -E 'context-bonsai-(prune|retrieve)'` (non-empty; build-level only — runtime registration is Phase 6's live check).
18. Baseline-delta gate: post-replay must-be-green rows green; net-new failures vs baseline are hard-fail regressions.
19. Change-scope + generated-artifact checks, from `WORKTREE`:
    - `git diff --name-status 637f7dd6d737f3961e6bf32fbb3861c4953269c5..HEAD` — every path in the union of replay-set `target_paths`; an out-of-scope landing without a prior exception → STOP + revert (`executor-scope-violation`).
    - `git diff --name-status 637f7dd6d7..HEAD | grep -E '(\.d\.ts$|/generated/|/__generated__/|openapi|\.gen\.|/dist/)' | wc -l` → `0` (§2.7).

### Phase 6: E2E release gate — live, required (§1.13 gate 11, §4.5, `docs/codex-e2e-runbook.md`)

Commands are **copied from `docs/codex-e2e-runbook.md`** (no invention; a missing command is a `structural-missing-command-binding` flag, not a license to improvise). Timeout wrap `timeout 240` on every LLM-invoking command; exit 124 → scenario `BLOCKED`, not `FAIL`. Evidence: `mkdir -p /tmp/codex-bonsai-e2e`; one JSONL capture per drive, `/tmp/codex-bonsai-e2e/<scenario>.jsonl`. The runtime under test is `WORKTREE`'s freshly built `codex-rs/target/debug/codex` — never the operator's installed `codex`. Session id for resume turns: `SESSION_ID=$(head -1 <evidence-file> | jq -r 'select(.type == "thread.started") | .thread_id')`. Archive inspector: `grep -l '"bonsai_archive"' ~/.codex/sessions/*/*/*/rollout-*.jsonl` then `jq -c 'select(.type == "compacted") | .bonsai_archive' <rollout-file>` (read-only; never edit or commit rollout files).

Required scenario set (immutable per cycle, §4.5): live tool registration; E2E-01 (contiguous prune); E2E-02 (ambiguity rejection); E2E-03 (retrieve by anchor); E2E-07/Protocol A; **E2E-04 (gauge) and E2E-06 (persistence across resume) — the port's first live runs of both, not skippable** (runbook "Open live scenarios"; grep prior records first per the runbook's first-run check). Drives per the runbook: E2E-01/02 via invented-marker prune + resume follow-ups; E2E-03 passing the exact anchor id from E2E-01's tool result; E2E-07 per the oracle method (secret never quoted in prompts or tool args); E2E-04 in a **fresh session** (not shared with E2E-01/02/03): drive exactly six user turns — turns 1–4 trivial single-line filler (e.g. one-word arithmetic answers, no tool use requested), turn 5 the cadence turn, turn 6 asking the model to quote verbatim any context-pressure guidance it currently sees, with an off-cadence control quote-check on turn 2 showing no gauge (codex's `user_turn_number` is recomputed from persisted history each turn — `codex-rs/core/src/hook_runtime.rs` `bonsai_gauge_inputs_for_turn` — so resume-per-turn drives are safe, unlike cline's per-process counter); E2E-06 by ending the process after a prune, then `codex exec --json resume <session-id>` with placeholder quote + retrieve, plus the `bonsai_archive` rollout entry surviving reload.

Verdict semantics: `BLOCKED` only on recorded reason codes (timeout exit 124, missing credentials, environmental preconditions) — a **genuine FAIL of any required scenario is a cycle STOP**: no retries beyond the procedure doc's own drive rules, no workarounds, no narrowing; record `generation-plan-defect` per §1.17 (or `structural-protocol-a-regression` if Protocol A fails on the clean build after all prior gates passed) and escalate. E2E-05 stays justified non-live (§4.5; the side-crate `cargo test` row carries its fail-closed path). Results doc: `codex_context_bonsai/docs/e2e-results-<DATE>-live.md` (collision-suffix rule per Planned Targets), committed in the side crate.

### Phase 7: Release-gate steps 1–4 + seal gates (§2.9, §1.13)

20. §2.9 step 1 — tag the rebase point: from `WORKTREE`, `git tag bonsai/v1-on-codex-0.125.0` at the replayed tip.
21. §2.9 step 2 — e2e behavioral gate: Phase 6 evidence (Protocol A required always).
22. §2.9 step 3 — parent pin advance, local, on `pin-advance/codex-0.125.0`. **Branch base is bound**: create the branch from the parent `HEAD` at execution start — `git checkout -b pin-advance/codex-0.125.0` with no other checkout first. (The parent working line is the branch containing this plan's commit — at generation, `pin-advance/cline-2.17.0`; do **not** switch to `main`, which lacks the sealed prior cycles' local landings.) The parent checkout's `codex/` submodule moves to the replay tip by detached tag checkout (`git -C codex checkout --detach bonsai/v1-on-codex-0.125.0` — the replay branch itself is checked out in the worktree). **Staged paths are bound**, exactly: `codex` (the moved pin), `codex_context_bonsai` (the moved pin, after the e2e results commit lands there in Phase 6), `.agents/plans/validation/baseline-5a680bcb3a5d63dffd7b77a4cdd414b2c951ab9c-on-rust-v0.125.0.json`, `.agents/plans/validation/exceptions-5a680bcb3a5d63dffd7b77a4cdd414b2c951ab9c-on-rust-v0.125.0.json` — one commit, nothing else staged.
23. §2.9 step 4 — pre-publish install gate: run the installation e2e in pre-publish mode using **exactly** the runbook Part 2 block "Pre-publish local sourcing — concrete commands" (`file://` sourcing — the codex fork is shallow and bundles from it are unclonable; substitute `<parent-pin-advance-branch>` = `pin-advance/codex-0.125.0`), then the Phase 3/4 registration + smoke drives from the fresh tree. If the runbook lacks any needed command, flag `structural-missing-command-binding` — never invent. **On real disk, never `/tmp`**; remove `$GATE` and unset the three `uploadpack.allowReachableSHA1InWant` keys at gate end. PASS authorizes step 5 **as a separate owner-approved act**; FAIL → fix the README/install path and re-run; BLOCKED → resolve and re-run. Never proceed on FAIL/BLOCKED.
24. Seal gates 1–13 (§1.13), each machine-checkable: 14/14 rows mapped; zero hard-blocking rows; zero unapproved late fixes; replay-set checksum re-verified (`2fe527f22887…`); no separate approval artifacts required (zero `manual_review` rows); baseline complete; provenance-trailer count 14 + final-state checks (`git merge-base 637f7dd6d7 HEAD` = `637f7dd6d7…`; `git rev-list --count 637f7dd6d7..HEAD` = 14; subjects match); post-replay ≥ baseline; change-scope union; no unresolved exceptions; e2e PASS evidence; spec-immutability assert; reviewer+judge approvals. Push-readiness dry-runs (§2.6) are deferred to the owner-approved publish continuation, with the pushes they precede.

### Phase 8: Cleanup — §1.19 scratch-state lifecycle (on BOTH seal and STOP paths)

The removal set is exactly the paths this run creates. Runs are serial; never remove another run's state.

- **On seal** (after the branch and tag are recorded in the fork repo — the refs, not the worktree, are durable): remove the worktree (`git -C $PARENT/codex worktree remove --force $PARENT/codex/.agent_tmp/rebase-on-rust-v0.125.0`), the shim symlink if it survives (`rm -f $PARENT/codex/.agent_tmp/codex_context_bonsai`), the e2e scratch (`rm -rf /tmp/codex-bonsai-e2e`) after the results doc is committed, and the install-gate real-disk scratch dir. The replay branch stays (it is the tag's branch ref, rewritten next cycle per DEVELOPMENT.md chain discipline).
- **On STOP**: the run's scratch is run evidence — preserve until the STOP is dispositioned, then the disposition act removes it. Record the STOP reason code (§1.17) in the final and §1.16 maintenance reports.
- Parent-repo validation artifacts (plan, replay set, baseline, exceptions, rehearsal logs) are durable cycle records, not cleanup targets.

## Validation Commands, grouped by working directory (§1.14 item 8)

### From `PARENT`
- `git status --short` (only enumerated submodule-pin lines + this cycle's own artifacts)
- `jq -c < .agents/plans/validation/replay-set-5a680bcb3a5d63dffd7b77a4cdd414b2c951ab9c-on-rust-v0.125.0.json | sha256sum | cut -d' ' -f1` → `2fe527f22887571bbe1db92274fccd94de895df5f14155acb4d064082749b651`
- `test -f docs/agent-specs/forward-port-spec.md && test -z "$(git diff --name-only -- docs/agent-specs/forward-port-spec.md)"` (or the hash re-assertion form)

### From `CODEX` (read-only inventory + frozen identities)
- `git fetch upstream "refs/tags/rust-v0.125.0:refs/tags/rust-v0.125.0"`; `git rev-parse --verify 'refs/tags/rust-v0.125.0^{commit}'` → `637f7dd6d737f3961e6bf32fbb3861c4953269c5`
- `git rev-parse --verify refs/heads/feat/spec-compliance` → `5a680bcb3a5d63dffd7b77a4cdd414b2c951ab9c`
- `git merge-base 637f7dd6d737f3961e6bf32fbb3861c4953269c5 5a680bcb3a5d63dffd7b77a4cdd414b2c951ab9c` → `1fda843fbcd825d92f41005060a181ebda615ff7`
- `git log --topo-order --reverse --format='%H|%P|%s' 637f7dd6d7..5a680bcb3a` (14 rows); `git log --name-status --find-renames --format='%H|%s' 637f7dd6d7..5a680bcb3a`; `git cherry -v 637f7dd6d7 5a680bcb3a` (14 `+`); `git log --merges 637f7dd6d7..5a680bcb3a` (empty)

### From `WORKTREE` root / `WORKTREE/codex-rs`
- `git rev-parse HEAD` → `637f7dd6d7…` (before replay)
- `cargo build --bin codex` (from `codex-rs`; r01 / post-replay)
- `cargo test -p codex-core context_bonsai` (from `codex-rs`; post-replay)
- `strings codex-rs/target/debug/codex | grep -E 'context-bonsai-(prune|retrieve)'` (from root; post-replay, non-empty)
- `test "$(git log --format='%b' 637f7dd6d737f3961e6bf32fbb3861c4953269c5..HEAD | grep -cE '^\(cherry picked from commit [0-9a-f]{40}\)$')" = "14"`
- `git merge-base 637f7dd6d7 HEAD` → `637f7dd6d7…`; `git rev-list --count 637f7dd6d7..HEAD` → `14`; `git log --format='%s' 637f7dd6d7..HEAD` (subjects match)
- `git diff --name-status 637f7dd6d7..HEAD` (⊆ target_paths union); generated-artifact probe → `0`

### From `SIDE` (`codex_context_bonsai/`)
- `cargo test` (r03, must-be-green); `cargo fmt --check` (r04, informational); `cargo clippy --all-targets --all-features` (r05, must-be-green)

## E2E Gate section (§1.14 item 9)

Procedure: `codex_context_bonsai/docs/e2e-testing.md` (combined installation+runtime; binds all eight e2e-spec slots). Command binding: `docs/codex-e2e-runbook.md` — copy commands; flag gaps, never invent (§4.5 rule; no gap encountered at generation). Required minimum for the release-gate PASS (immutable, §4.5): live registration, E2E-01, E2E-02, E2E-03, E2E-07/Protocol A, plus the **first live E2E-04 and E2E-06**. E2E-05 stays justified non-live. Evidence recording: `codex_context_bonsai/docs/e2e-results-<DATE>-live.md` (collision-suffix rule); stdout captures stay in `/tmp/codex-bonsai-e2e` until removed per §1.19; rollout files are never committed.

## Worktree Artifact Check (§1.14 item 10)

- Checked at generation: 2026-07-06 (owner-tier generator).
- **Parent-repo generator writes**: this plan; the replay-set JSON; the two rehearsal logs. Overlap: no pre-existing artifact at these `-on-rust-v0.125.0` paths (`tracked-dirty`: none; `existing-untracked`: none). The Stage 6 cycle's unsuffixed artifacts at the same `SOURCE_HEAD_SHA` are a **different closed cycle**, handled by the §1.14 same-source different-target rule — untouchable history, not a collision.
- **Executor writes**: baseline/exceptions JSONs — do not yet exist; the executor re-runs this check immediately before its first write.
- **Worktree targets**: created fresh in the new worktree; `TARGET_WORKTREE` and `TARGET_BRANCH` do not exist at generation (the rehearsal used the distinct `rehearsal-rust-v0.125.0` worktree and `rehearsal/on-rust-v0.125.0` branch, both removed at loop close). The shim path `codex/.agent_tmp/codex_context_bonsai` persists from the Stage 6 cycle, content-identical to what Phase 2 creates — verify with `readlink` and reuse; not a collision.
- Escalation status: none. Decision citation: routine-path generation by the owner tier under the standing DRIVE order; target selection recorded in Frozen Inputs.

## Plan Approval and Commit Status (§1.14 item 11)

- **Approval Status**: approved (routine local landing; execution of Phases 0–8 is in-scope routine work; §2.9 steps 5–6 pushes stay owner-gated).
- **Approval Citation**: standing owner order 2026-07-05 — local landings are routine and proceed autonomously; only outward actions wait. Reviewer+judge gates satisfied by the owner-tier generation + the §1.15 loop below.
- **Plan Commit Hash**: the commit introducing this file and the replay set (resolve with `git log --follow --oneline -1 -- .agents/plans/story-rebase-cycle-5a680bcb3a5d63dffd7b77a4cdd414b2c951ab9c-on-rust-v0.125.0.md`).
- **Ready-for-Orchestration**: YES — the §1.15 loop closed at iteration 1 with zero blocking findings remaining (below).

## Validation Loop Results (§1.15)

**Iteration 1 — target-resolution rehearsal (2026-07-06, owner tier): GREEN.** Disposable scratch worktree `codex/.agent_tmp/rehearsal-rust-v0.125.0` at `UPSTREAM_HEAD_SHA` (distinct from `TARGET_WORKTREE`), shim verified. Full replay sequence applied: `git cherry-pick -x` of all 14 rows in topological order — **two conflicts**, exactly the ones bound in Phase 4 (row `96bcf61e` `session.rs` constructor relocation; row `a17bc74e` `handlers/mod.rs` import rename), both resolved inside their rows' `target_paths`; the other 12 rows applied clean. Provenance-trailer count 14/14; `git merge-base 637f7dd6d7 HEAD` = `637f7dd6d7…`. Post-replay rows: `cargo build --bin codex` exit 0 (2m23s, plain command, no rig accommodation); `cargo test -p codex-core context_bonsai` 38 passed 0 failed; `strings` registration probe non-empty (both tool names); change-scope diff ⊆ `target_paths` union; generated-artifact probe 0. Side-crate rows from `codex_context_bonsai/`: `cargo test` exit 0 (38 passing across targets); `cargo fmt --check` 233-line pre-existing drift (r04 informational baseline, §4.5); `cargo clippy --all-targets --all-features` exit 0. Evidence logs: `.agents/plans/validation/rehearsal-codex-on-rust-v0.125.0.log`, `.agents/plans/validation/rehearsal-codex-sidecrate-on-rust-v0.125.0.log`. Rehearsal worktree and its `rehearsal/on-rust-v0.125.0` branch removed at loop close per §1.19; the shim symlink retained deliberately for the execution phase (Worktree Artifact Check).

**Iteration 1 — missing-details pass** (independent, repository-inspecting): ZERO BLOCKING findings. The reviewer independently re-verified every frozen SHA (tag, source head, merge-base, 14/14 cherry `+`, zero merges, 48-commit drift count, the 0.124.0-vs-0.125.0 base-containment rationale), the replay-set schema/field order and checksum, both bound conflict shapes against real `git show` output, every cited file and spec section, toolchain presence, shim resolution, no-collision status of worktree/branch/tag names, and `codex login status` exit 0. One informational observation (plan artifacts untracked at review time) was subsumed by ambiguity finding (1)'s fix.

**Iteration 1 — ambiguity pass** (independent, repository-inspecting): 3 blocking + 1 minor, all fixed in this plan. (1) The dirty-path enumeration's commit precondition had no bound owner/step → Phase 1 step 2 now binds the invoker as the committer, pre-launch, with an executor STOP on violation. (2) "Parent's current working-line tip" was resolvable two ways (current branch vs `main`) and the pin-advance commit's staged paths were prose-only → step 22 now binds the branch-creation command, forbids switching to `main`, and enumerates the exact staged paths. (3) The install gate's bundle commands existed nowhere (runbook narrated, never bound) and the plan lacked a no-invention guard → the runbook gained the concrete "Pre-publish local sourcing" block (owner-tier fold; `file://` realization because the shallow fork makes bundles unclonable — clone-failure verified live), and step 23 now cites it verbatim with a `structural-missing-command-binding` guard. Minor: E2E-04's turn count, filler content, session-freshness, and off-cadence control were unbound → bound in Phase 6; the reviewer verified codex's turn counter is persisted-history-derived (no cline-style per-process reset trap). The reviewer also verified naming vs §4.5 and §1.19 cleanup-row coverage clean.

**Loop closed at iteration 1** (zero blocking findings remaining after fixes; rehearsal GREEN on the first pass; both reviewer passes recorded above).

## Completion Checklist (mirrors the seal gates)

- [ ] Every inventory item mapped to exactly one replay-set row (14/14)
- [ ] Zero unresolved `manual_review` / hard-blocking rows
- [ ] Zero unapproved late fixes; no source drift
- [ ] Replay-set present, schema-valid, checksum verified pre-replay and unchanged at seal
- [ ] Baseline artifact complete; must-be-green rows green; `r02` = missing-as-expected; `r04` recorded informational
- [ ] Replay verified by provenance trailers (14) + commit-range final-state checks
- [ ] Post-replay validation no worse than baseline; must-be-green rows green
- [ ] Change-scope union check passes; generated-artifact probe = 0
- [ ] No unresolved exception records
- [ ] E2E release gate PASS (required set incl. first live E2E-04 and E2E-06); results doc committed
- [ ] §2.9 steps 1–4 complete (local tag, e2e, local pin advance, pre-publish install gate); steps 5–6 explicitly deferred to owner approval
- [ ] Spec immutability asserted
- [ ] Reviewer + judge approvals recorded
- [ ] §1.19 cleanup rows executed on the realized path (seal or STOP)

## §1.16 Maintenance duties (run after cycle end — sealed or STOP)

The executor performs routine maintenance and records the outcome (a STOP does not waive it):
- Absorb slot-level facts that changed into §4.5 with cited evidence; escalate core/shape (Parts 1–3) gaps to the owner tier. The routine executor edits Part 4 only; the edit is left uncommitted and its path named in the final report — the next cycle receives it as an enumerated dirty path until owner review lands.
- Give every stumble a `SPEC-GAP` vs `EXECUTOR-FAIL` verdict.
- If no slot-level fact changed, record that explicitly.
- Known candidates at generation: none open — the Stage 6 cycle's three SPEC-GAPs were folded at owner tier before this plan was generated (`codex-rs/Cargo.lock` allowlist, §1.11 git-fork pressure-test semantics, §2.1 raw-commit substitution), and the §1.14 same-source different-target rule landed as this cycle's generation-time owner-tier amendment.
