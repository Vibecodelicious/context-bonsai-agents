# Observer Log: GPT-5.5 Pilot — OpenCode v1.17.13 Cycle, Run 4

Maintained by the observing (Fable-tier) session, not by the pilot executor. Records failure-attribution verdicts per `docs/meta-loop-direction.md`: every pilot stumble gets `SPEC-GAP` (the spec left a judgment call unresolved — fix the artifact) or `EXECUTOR-FAIL` (the spec was deterministic and the executor still failed). The executor's own friction log is `gpt55-v1.17.13-friction-log.md`; entries here reference it. This file is untracked and gitignored by design; run 1's record is committed at parent `20d5d40`, run 2's at `f9d9063`, run 3's at `bddcf96` (on-disk copies under `archive/run2-v1.17.13/` and `archive/run3-v1.17.13/`).

**Decision provenance**: watchdog branch-1 decision 2026-07-03 ~04:30 UTC (run 4 under unchanged terms, owner slacked with a pre-launch override window), recorded in the direction doc's Decision record. Launch prep: run-3 residue archived and cleared (§1.14 glob clean), exit-marker plumbing re-verified by dry run, 429 self-resume expectation added to `launch-run.sh`.

Run-4 acceptance test (unchanged four clauses): the cycle seals through the e2e gate or halts at an identified fail-closed STOP; every stumble carries a verdict; §1.16 maintenance attempted and its outcome recorded in either outcome; no recurrence of any of the **eight** previously fixed SPEC-GAPs — run 1's three (tag fetch, dirty-path enumeration, maintenance-on-STOP), run 2's three (detached-plugin publish, pin advance vs occupied branch, install-gate record disposition; fixed at `3aaea2f`), run 3's two (runtime-e2e and install-gate command bindings, V2/V3; closed by `docs/opencode-e2e-runbook.md` at `2f8495a` plus §4.2's flag-don't-invent rule at `15c0463`). A recurrence is a defect in the owner-tier fix, not new pilot data. The isolated capability question: does plan generation converge — or flag a missing binding early and explicitly — now that no known spec gap taxes the §1.15 loop budget?

Standing observation protocol: no interference (verdicts only, no hints, no edits to anything the executor reads while the run lives); wake-and-verify every ≤30 minutes (process alive, run log growing, or explained); on any silent stall, check the newest `~/.local/share/opencode/log/*.log` for `session.processor` errors — a mid-run provider 429 is EXPECTED behavior: the session sleeps out the retry-after in-process and self-resumes at quota reset (run-3 precedent). Verify self-resume at the retry-after horizon before considering any injection; injection requires watchdog sanction.

Format per entry: UTC timestamp, what happened (with friction-log or run-log reference), verdict, evidence for the verdict.

## Fix-recurrence checks

Checked post-exit against the run log, the generated plan (`story-rebase-cycle-0dfbeeda…​.md`), and the executor's friction log. **No recurrence of any of the eight fixed SPEC-GAPs.**

- Run-1 three: preflight passed clean before the plan phase began (release range enumerated, §1.14 globs empty — tag fetch and dirty-path enumeration held); §1.16 maintenance ran on the STOP (third consecutive run).
- Run-2 three: the run never reached execution, so these are checked as plan content. The plan carries the spec's bound branch names and side-repo commit rules (plan lines 39, 298); the residual reviewer complaints about pin-bump concreteness are transcription defects against a spec that does bind the requirement (§4.2 evidence-paths clause) — the slot itself held.
- Run-3 two (V2/V3 command bindings): the runbook exists and the executor found it at launch (+20s, first milestone below). Iteration-2 shows the executor pasting concrete commands from it (plan line 464). The residual iteration-3 defects are infidelity to the runbook's content, not absence of it — checked directly: the runbook's "verbatim" `insteadOf` block covers the HTTPS submodule URLs the plan's hand-rolled variant missed. The binding held; the executor departed from it.

## Milestones

- 2026-07-03 03:36 UTC — **RUN 4 LAUNCHED** in tmux window `bonsai:@4` (`gpt55-pilot-run4`) via `launch-run.sh`; orchestrator PID 1863670. Owner said "launch it, window's yours to hold" after the 15-minute override slack (send verified `ok`), waiving the remaining window. First wake-and-verify at +20s: run log live and growing; the executor created its todo list and is reading the planning guidance, the e2e spec, and — notably — `docs/opencode-e2e-runbook.md`, i.e. it found the new §4.2 command binding immediately. No verdicts; nothing to attribute yet.

- 2026-07-03 04:08 UTC — **QUOTA STALL (expected)**: 29-minute sweep found the run log silent since 03:52. The pilot's opencode log (`2026-07-03T033604.log`) shows `session.processor error=Subscription quota exceeded. Retry in 3hr 43min` at 03:52:15, raised on a `bonsai-reviewer` subagent LLM call (session `ses_0d9e62167ffe...`). This is the run-3 429 pattern the launch prep anticipated; process alive (PID 1863670), self-resume expected at the retry-after horizon **~07:35 UTC**. No verdict — environmental, not a pilot stumble. Progress before the stall: story plan drafted and iterated (multiple `apply_patch` edits to `story-rebase-cycle-0dfbeeda…​.md`), friction log written to, reviewer agents mid-flight on "Missing details final" / "Ambiguity final" passes. Context note: a second, unrelated opencode instance (`opencode --continue`, PID 1885728, in `opencode_context_management`) is active on the same machine and may share the provider subscription; noted for quota-budget context only, not attributed.

- 2026-07-03 07:35 UTC — **SELF-RESUME CONFIRMED**: run log grew at 07:36:20 (9870 → 11388 bytes); the pilot's opencode log shows `session.processor` activity resuming 07:35:33, within a minute of the 03:52:15 + 3h43m retry-after horizon. Second consecutive validated in-harness 429 recovery; zero observer contamination (four wake-and-verify sweeps during the stall, 03:38–07:07, read-only throughout).

- 2026-07-03 07:36 UTC — **RUN 4 EXITED, `PILOT-PROCESS-EXITED code=0`**: fail-closed STOP at forward-port-spec §1.15 — the third reviewer pass still reported blocking findings, so the cycle stopped before plan approval, commit, replay, and all gates. Zero commits in parent, `opencode/`, `opencode_context_bonsai_plugin/`. On resume the executor consumed the final reviewer results, wrote the STOP entry, ran §1.16 maintenance (`gpt55-v1.17.13-maintenance.md`, no Part 4 edit), wrote the final report, and exited cleanly — under a minute of post-resume work. Plan artifacts left uncommitted for review, as the report states.

## Verdicts

- **V1 — §1.15 exhaustion, clean EXECUTOR-FAIL** (2026-07-03 07:36 UTC; friction-log entries 03:44:11, 03:49:58, 07:35:48). The three iteration-3 residual blocking findings were each checked against the owner artifacts, and every one was bound before the run started:
  1. *HTTPS submodule rewrites*: `docs/installation-e2e-template.md` line 110 binds a single prefix rule — `url./tmp/bundles/.insteadOf https://github.com/Vibecodelicious/` — that covers every URL in `.gitmodules` (all HTTPS). The runbook incorporates it "verbatim". The plan (lines 309–311) instead hand-rolled three per-repo rules and wrote the two submodule rules against `git@github.com:` SSH URLs that appear in no owner artifact. Bound, departed from, defect introduced.
  2. *Concrete pin-bump commands*: spec §4.2's evidence-paths clause binds the requirement and its position ("the parent working branch must also carry the side repo's submodule pin bump before §2.9 step 5"); the plan restates it as prose (lines 298, 430) without the elementary git commands the reviewers required. Bound in substance; under-transcribed.
  3. *Install-gate result ordering*: same clause plus the runbook's Result recording section state the order (side-repo result commit → parent pin bump → §2.9 step 5). The plan left it ambiguous through three passes.
  The flag-don't-invent rule was never legitimately triggerable — no binding was missing — and the executor never flagged one (correct); its failure mode was transcription infidelity and under-specification against bindings it had found and read. The executor's own attribution (`EXECUTOR-FAIL` in plan generation, maintenance report) matches this verdict — the third consecutive run with executor/observer attribution agreement. Contrast with run 3: there the executor fixed every self-caused finding within budget and iteration 3 blocked only on spec gaps; here, with those gaps closed, iteration 3 blocked only on executor-caused defects. This is the unconfounded answer run 4 was launched to produce.

## Acceptance assessment

All four clauses **PASS**; the run is a valid, unconfounded data point.

1. *Seal or identified fail-closed STOP*: STOP at §1.15, correctly identified, cleanly reported, zero commits.
2. *Every stumble carries a verdict*: one stumble class (§1.15 exhaustion), verdict V1 above; the mid-run 429 is environmental, no verdict (run-3 precedent).
3. *§1.16 attempted and recorded*: yes — maintenance report written on the STOP path, correctly concluded no Part 4 edit was warranted (the findings were plan defects, not slot evidence), correct escalation discipline for the third consecutive run.
4. *No recurrence of the eight fixed SPEC-GAPs*: confirmed per the Fix-recurrence checks section.

**Answer to the isolated capability question** (does plan generation converge, or flag a missing binding early, when no known spec gap taxes the loop budget?): **No convergence.** With nothing to flag and every needed binding present, the executor still exhausted the three-pass budget on self-caused transcription defects. This is the first direct contrary evidence on capability question (a), reversing run 3's tentative positive signal — and it is exactly the clean reading the decision record said a run-4 exhaustion on deterministic content would deliver.
