# Story: Forward-port cycle — Codex Context Bonsai onto upstream base `1fda843fbc` (Stage 6 pressure test)

Routine forward-port cycle per `docs/agent-specs/forward-port-spec.md` (Part 1 core, Part 2 shape binding — git-fork, §4.5 Codex slot table). Generated by the owner tier on 2026-07-05 from the spec plus the §4.5 slots. This is the **first routine cycle for the Codex harness** and the Stage 6 acceptance-gate plan (`derivation-pipeline-spec.md` §9): the §4.5 binding is proven by the routine path consuming it. All relative paths are from the parent repo root (`context-bonsai-agents/`); worktree/side-repo commands state their working directory.

## Goal

Replay the Codex Context Bonsai port — the contiguous 14-commit `[CB-codex.1]`/`[Story CO1]`/`[Story CO2]` suffix currently tipped at `SOURCE_HEAD_SHA=5a680bcb3a5d63dffd7b77a4cdd414b2c951ab9c` on `feat/spec-compliance` — onto the frozen upstream base commit `UPSTREAM_HEAD_SHA=1fda843fbcd825d92f41005060a181ebda615ff7` in an isolated worktree, and prove the §4.5 git-fork machinery (deterministic inventory, classification, cherry-pick replay, canonical validation set) runs clean against it. This is an `uncommitted-pressure-test` cycle: no upstream release is pending, so the target is the port's own current consumed base. Nothing is pushed and no durable ref (branch/tag) is landed; the worktree replay commits are scratch verified by working-tree commands (§1.11).

## Non-Goal

- No pending-release forward-port: the port already sits on `1fda843fbc`; this cycle does not advance the port onto a newer `rust-v*` tag.
- No live e2e: per §1.15's rehearsal scope and the pressure-test purpose, the E2E gate rows are **bound but not run** (marked `not-run-in-pressure-test`). Live e2e (including the two open scenarios E2E-04/E2E-06) is out of scope here.
- No publish ladder (§2.9): no tag, no pin advance, no push, no `main` fast-forward.
- No replay/rewrite of the source branch in place; all replay happens in the scratch worktree.
- This plan is generated only. Generation does not create the worktree, build, or commit anything; the executor performs those on execution.

## Execution Outcome Statement

After execution, the scratch worktree's `HEAD` is based on `UPSTREAM_HEAD_SHA` (`1fda843fbc`) and contains exactly the 14 replayed commits from the approved replay set in the listed topological order — verified by cherry-pick provenance trailers and working-tree/diff commands, never by source-SHA equality. No durable branch or tag is pushed; the worktree and its side-crate symlink shim are removed at seal (§1.19). The pressure test passes when the §1.15 generation validation loop (including the target-resolution rehearsal) returns zero blocking findings and the post-replay canonical validation set is green in rehearsal.

## Frozen Inputs (§1.1)

Invoker-supplied, frozen at cycle start; never re-resolved. All commands reference the frozen values below.

- `SOURCE_REF`: `refs/heads/feat/spec-compliance`
- `SOURCE_HEAD_SHA`: `5a680bcb3a5d63dffd7b77a4cdd414b2c951ab9c`
- `UPSTREAM_REF`: `1fda843fbcd825d92f41005060a181ebda615ff7` — a **raw frozen commit**, upstream `main` at the port's base. **Rationale for the §2.1 tag substitution**: §2.1 mandates a release tag (or an explicitly-targeted branch ref). For this Stage 6 pressure test no upstream release is pending; the acceptance target is the port's current consumed base, which sits directly on upstream `main` *between* releases (the newest reachable stable release tag is `rust-v0.0.2504301132`, which is not the port's base). A raw frozen commit therefore stands in for §2.1's release tag for this pressure test only. This is a recorded spec deviation (candidate SPEC-GAP; see Classification note and §1.16 maintenance).
- `UPSTREAM_HEAD_SHA`: `1fda843fbcd825d92f41005060a181ebda615ff7` (identical to `UPSTREAM_REF`; the ref is already a full 40-char SHA)
- `BASE_SHA`: `1fda843fbcd825d92f41005060a181ebda615ff7` — **computed, not asserted** (§2.1). `git merge-base 1fda843fbc… 5a680bcb3a…` returned `1fda843fbcd825d92f41005060a181ebda615ff7` (confirmed at generation, after `--unshallow`/`--deepen`; see Classification). `BASE_SHA == UPSTREAM_HEAD_SHA`: the port suffix sits directly on the base with no intervening upstream history in range.
- `TARGET_WORKTREE`: `/home/basil/projects/context-bonsai-agents/codex/.agent_tmp/rebase-on-1fda843fbc`
  - **Path-dependency symlink shim** (§4.5 Naming): before any build in the worktree, create the sibling symlink `codex/.agent_tmp/codex_context_bonsai` → `../../codex_context_bonsai`. The fork's `codex-rs/core/Cargo.toml` declares `codex-context-bonsai = { path = "../../../codex_context_bonsai" }`; from `.agent_tmp/rebase-on-.../codex-rs/core` that resolves to `.agent_tmp/codex_context_bonsai`. The symlink is step-scoped scratch, removed with the worktree per §1.19.
  - Naming note: §4.5 keys the worktree name on the upstream tag including its `rust-v` prefix; since this pressure test targets a raw commit, the worktree suffix is the short frozen commit `1fda843fbc` (deterministic from the frozen input). Recorded here so the name is executable without re-derivation.
- `TARGET_BRANCH`: `replay/context-bonsai-on-codex-1fda843fbc` — scratch worktree branch (bare identifier; §4.5 keys branch/tag on the bare version without `rust-v`, and for this raw-commit pressure test the short base commit is the version stand-in). **Not pushed.**
- `TARGET_TAG_AT_TIP`: `bonsai/v1-on-codex-1fda843fbc` — **not created and not pushed in this pressure-test cycle** (§2.9 ladder is out of scope). Named here for completeness only.
- `PARENT_PIN_ADVANCE_BRANCH`: `pin-advance/codex-1fda843fbc` — **not created** (no pin advance in pressure test).
- `VALIDATION_MODE`: `uncommitted-pressure-test` (§1.11)
- Range scope: `UPSTREAM_HEAD_SHA..SOURCE_HEAD_SHA` (default; no author-filter override)
- Upstream commits ahead of `BASE_SHA` in range: 0 (the range is exactly the 14 port commits)
- Supersession lineage: none (first Codex cycle; no prior plan superseded)

## Allowlists and Planned Targets (§1.14 item 3)

Per §4.5, no overrides for this cycle:

- **Runtime allowlist**: fork paths `codex-rs/core/**`, `codex-rs/tools/**`, `codex-rs/hooks/**`, `codex-rs/protocol/**`, `codex-rs/rollout/**`, `codex-rs/app-server-protocol/**`; side-crate paths `codex_context_bonsai/{src/**,tests/**,Cargo.toml,Cargo.lock}`.
- **Docs allowlist**: `.agents/plans/**`, `codex_context_bonsai/docs/**`.
- **State-only**: metadata/state artifacts outside both.
- **Planned worktree targets (replay only, not direct edits)**: the union of replay-set `target_paths` (see Classification / replay-set artifact). All are cherry-pick replay targets inside the runtime allowlist, with the one exception noted below.
- **Planned parent-repo targets (generator writes)**: this plan file; `.agents/plans/validation/replay-set-5a680bcb3a5d63dffd7b77a4cdd414b2c951ab9c.json`. **Executor writes (parent repo)**: `.agents/plans/validation/baseline-5a680bcb3a5d63dffd7b77a4cdd414b2c951ab9c.json`; `.agents/plans/validation/exceptions-5a680bcb3a5d63dffd7b77a4cdd414b2c951ab9c.json` (empty by default).
- **Explicit non-targets** (§4.5): this spec (`docs/agent-specs/forward-port-spec.md`); the source branch `feat/spec-compliance` in place; `opencode/`, `pi/`, `pi_context_bonsai/`, `tweakcc_context_bonsai/`, and every other harness repo; `~/.codex/**` auth and live user sessions (rollout files read for evidence only, never edited/committed); prior cycles' worktrees; the interleaved upstream commits on the port branch (none in this range — all 14 are port content).

**Allowlist exception (candidate SPEC-GAP #1)**: rows `26e6abb7` and `fd56b70e` each touch `codex-rs/Cargo.lock` — the fork *workspace* lockfile, regenerated by their `Cargo.toml` dependency edits. §4.5's runtime allowlist enumerates the six fork crate directories and the *side-crate* `Cargo.lock` (`codex_context_bonsai/Cargo.lock`) but not `codex-rs/Cargo.lock`. The path is included in those rows' `target_paths` so the §1.13 gate-9 change-scope union covers it; classification defaults to `required_runtime` by first-match precedence (the commits touch runtime-allowlist paths). Candidate Part 4 slot fix: add `codex-rs/Cargo.lock` to the §4.5 runtime allowlist (see §1.16 maintenance). It is not a generated-SDK artifact (§2.7 excludes `.d.ts`/`dist`/`generated`), so it may ride the replay commits.

## Source Commit Classification (§1.14 item 4)

Inventory produced by the §2.2 command sequence (run read-only at generation against `codex/`, range `1fda843fbc..5a680bcb3a`): 14 commits, topological order, **zero merges** (`git log --merges` empty), all 14 marked `+` by `git cherry -v` (none patch-equivalent in upstream). Bucket counts: `required_runtime` 14; `already_in_upstream` 0; `drop` 0; `late_fix_pending` 0; `required_docs` 0; `state_only` 0; `manual_review` 0. No hard-blocking rows; the >50% `manual_review` structural threshold (§1.17) is not approached.

**Merge-base / shallow-checkout note**: `codex/` is a shallow clone (`git rev-parse --is-shallow-repository` → `true`). At generation the range and `git merge-base` resolved successfully (both endpoints and their common ancestor are within the fetched history); had merge-base failed with "no common ancestor," §4.5 requires deepening (`git fetch --deepen`/`--unshallow` from `upstream`) and treating the failure as an `env-*` condition, not divergence. The executor re-runs the deepen-if-needed preflight (Phase 1) before recomputing `BASE_SHA`.

Every row: `bucket=required_runtime`, `replay_action=cherry-pick`, `mapping_type=1:1`, `approver`=this plan's approval citation (no per-row hard-blocking approval needed; see Approval). `evidence` = `git cherry -v 1fda843fbc 5a680bcb3a` marks `+` and paths inside the runtime allowlist.

| source_sha | subject | bucket | replay_action | mapping_type | target_paths | evidence / rationale |
|---|---|---|---|---|---|---|
| `5cad90f9e70d12c4f760625acd09c071f0505e10` | [CB-codex.1] extend CompactedItem with optional bonsai archive payload | `required_runtime` | `cherry-pick` | `1:1` | `codex-rs/protocol/Cargo.toml`, `codex-rs/protocol/src/protocol.rs` | `codex-rs/protocol/**`; cherry `+` |
| `26e6abb721a5f2d46eaca4cc59a5ac92216c5136` | [CB-codex.1] add codex-core context_bonsai wrapper module | `required_runtime` | `cherry-pick` | `1:1` | `codex-rs/Cargo.lock`†, `codex-rs/app-server-protocol/src/protocol/thread_history.rs`, `codex-rs/core/Cargo.toml`, `codex-rs/core/src/compact.rs`, `codex-rs/core/src/compact_remote.rs`, `codex-rs/core/src/context_bonsai.rs`, `codex-rs/core/src/context_bonsai_tests.rs`, `codex-rs/core/src/lib.rs`, `codex-rs/core/src/session/rollout_reconstruction_tests.rs`, `codex-rs/core/src/session/tests.rs`, `codex-rs/rollout/src/metadata_tests.rs` | `codex-rs/core/**`, `codex-rs/app-server-protocol/**` (bonsai_archive: None in CompactedItem literals), `codex-rs/rollout/**`; introduces side-crate path dep; cherry `+`. †`Cargo.lock` = candidate SPEC-GAP #1 |
| `d94efcf17e289cb189b2bd2b6154f248e5f5fe41` | [CB-codex.1] harden context_bonsai wrapper guards and anchor ids | `required_runtime` | `cherry-pick` | `1:1` | `codex-rs/core/src/context_bonsai.rs`, `codex-rs/core/src/context_bonsai_tests.rs` | `codex-rs/core/**`; cherry `+` |
| `96bcf61ef68abd5aaa9aaa4fa69f4b3851c7484e` | [CB-codex.1] wire bonsai prune/retrieve through Session + SessionState | `required_runtime` | `cherry-pick` | `1:1` | `codex-rs/core/src/session/mod.rs`, `codex-rs/core/src/session/session.rs`, `codex-rs/core/src/session/tests.rs`, `codex-rs/core/src/state/session.rs` | `codex-rs/core/**`; cherry `+` |
| `a17bc74e7398cf4a72ecf77ebbd774c60536eb4d` | [CB-codex.1] register context-bonsai prune/retrieve in the tool registry | `required_runtime` | `cherry-pick` | `1:1` | `codex-rs/core/src/tools/handlers/context_bonsai.rs`, `codex-rs/core/src/tools/handlers/mod.rs`, `codex-rs/core/src/tools/spec.rs`, `codex-rs/tools/src/context_bonsai_tool.rs`, `codex-rs/tools/src/lib.rs`, `codex-rs/tools/src/tool_registry_plan.rs`, `codex-rs/tools/src/tool_registry_plan_types.rs` | `codex-rs/core/**`, `codex-rs/tools/**`; cherry `+` |
| `c9e69c0f392b66e16071b20b6c65bedf6a6aa586` | [CB-codex.1] cover bonsai archive payload through rollout reconstruction | `required_runtime` | `cherry-pick` | `1:1` | `codex-rs/core/src/session/rollout_reconstruction_tests.rs` | `codex-rs/core/**`; cherry `+` |
| `fd56b70e1728f86a27ee13e9213270a142be44c5` | [CB-codex.1] inject bonsai guidance + gauge context through hook events | `required_runtime` | `cherry-pick` | `1:1` | `codex-rs/Cargo.lock`†, `codex-rs/core/src/hook_runtime.rs`, `codex-rs/hooks/Cargo.toml`, `codex-rs/hooks/src/events/session_start.rs`, `codex-rs/hooks/src/events/user_prompt_submit.rs` | `codex-rs/core/**`, `codex-rs/hooks/**`; cherry `+`. †`Cargo.lock` = candidate SPEC-GAP #1 |
| `6b2f5a435b12f1bb6ed134426f8562b42c4755b8` | [CB-codex.1] adjust compact tests to account for bonsai guidance row | `required_runtime` | `cherry-pick` | `1:1` | `codex-rs/core/tests/common/context_snapshot.rs`, `codex-rs/core/tests/suite/compact.rs` | `codex-rs/core/**`; cherry `+` |
| `1beb7b2d9d874f4200f5bf537484b5124b79001f` | [CB-codex.1] Fix clippy regressions in context_bonsai module | `required_runtime` | `cherry-pick` | `1:1` | `codex-rs/core/src/context_bonsai.rs`, `codex-rs/core/src/context_bonsai_tests.rs` | `codex-rs/core/**`; cherry `+` |
| `689c4e1fc2117da22fade27c6ee33bdc20dc211c` | [CB-codex.1] Advance bonsai_turn_seq at user-turn boundary | `required_runtime` | `cherry-pick` | `1:1` | `codex-rs/core/src/session/mod.rs`, `codex-rs/core/src/session/tests.rs` | `codex-rs/core/**`; cherry `+` |
| `1863ca285a3406220537ae0dac5307aa91cea674` | [CB-codex.1] Skip bonsai guidance prepend on session resume | `required_runtime` | `cherry-pick` | `1:1` | `codex-rs/hooks/src/events/session_start.rs` | `codex-rs/hooks/**`; cherry `+` |
| `8e62af863edc1458509ff128611d1212b68ea85a` | [CB-codex.1] Rehydrate bonsai archive cache on resume | `required_runtime` | `cherry-pick` | `1:1` | `codex-rs/core/src/session/mod.rs`, `codex-rs/core/src/session/rollout_reconstruction_tests.rs` | `codex-rs/core/**`; cherry `+` |
| `78af9df233fdbf5896a873ff4ff273160423b7cb` | [Story CO1] Project all model-visible ResponseItem variants in extract_text | `required_runtime` | `cherry-pick` | `1:1` | `codex-rs/core/src/context_bonsai.rs`, `codex-rs/core/src/context_bonsai_tests.rs` | `codex-rs/core/**`; cherry `+` |
| `5a680bcb3a5d63dffd7b77a4cdd414b2c951ab9c` | [Story CO2] Populate is_prune_wrapper flag on Codex projection | `required_runtime` | `cherry-pick` | `1:1` | `codex-rs/core/src/context_bonsai.rs`, `codex-rs/core/src/context_bonsai_tests.rs` | `codex-rs/core/**`; cherry `+` |

Merge-commit scan: `git log --merges 1fda843fbc..5a680bcb3a` empty; no merge rows.

## Reviewer-Simplicity Evaluation (§1.14 item 5 / §2.5)

All 14 rows are `cherry-pick`. Because `UPSTREAM_HEAD_SHA == BASE_SHA`, the port suffix already sits directly on the replay base: cherry-picking each commit onto a worktree created at `1fda843fbc` reproduces the exact existing commits with no intervening upstream drift, so conflicts are not expected. For each row `cherry-pick` is strictly preferred over `cherry-pick + minor fixups` and `re-implement`; the latter two review worse and are unjustified when the base is identical to the port's own base. The §2.5 re-implement escape is available only if a cherry-pick unexpectedly conflicts and cannot be resolved inside the row's `target_paths` — then STOP, record a reviewer-simplicity evaluation and behavioral-contract table, and obtain reviewer+judge approval before switching. No re-implement is anticipated.

## Re-Implementation Behavioral Contract (§2.5)

Not applicable (all rows `cherry-pick`). If a runtime row is forced to `re-implement`, this section must be populated for that row with the §2.5 fields (`source_primitive_or_intent`, `current_upstream_boundary`, `return_shape`, `runtime_bridge_pattern`, `allowed_mutation_surface`, `approved_metadata_schema`, `metadata_runtime_validation`, `atomicity_requirement`, `generated_artifact_decision`, `public_api_exposure_decision`, `validation_evidence`) before execution proceeds.

## Validation Mode (§1.11)

- Mode: `uncommitted-pressure-test`. No upstream release is pending; the target is the port's current consumed base (`1fda843fbc`).
- The executor cherry-picks the 14 commits in the **scratch worktree** (git-fork replay inherently creates commits in the isolated worktree — §2.5). Final verification uses working-tree/diff commands (`git diff --name-status`, `git diff --stat`, `git rev-list --count`, provenance-trailer grep, targeted `cargo` tests) against `UPSTREAM_HEAD_SHA..HEAD` **in the worktree**; **nothing is pushed and no durable branch/tag is landed**.
- **Pressure-test seal-scope note (candidate SPEC-GAP #2)**: §1.11's literal "`HEAD` may still equal the frozen upstream / verification uses working-tree commands" language is written for the closed-artifact in-place shape (§3.5). The git-fork shape replays via cherry-pick, which necessarily advances the worktree `HEAD` past `UPSTREAM_HEAD_SHA`. Here "uncommitted-pressure-test" is bound as **"replay commits are scratch-only — unpushed, no durable ref landed, worktree removed at seal"** rather than a literally uncommitted working tree. Verification is by provenance + diff in the scratch worktree. Flagged for §1.16 owner-tier review.
- Locally regenerated artifacts (e.g. `codex-rs/Cargo.lock` churn beyond the committed rows, `target/` build output) are recorded as expected pressure-test drift, left uncommitted, and excluded by the final diff checks.

## Acceptance Criteria (§1.14 item 6)

- [ ] All replay work happens only in `TARGET_WORKTREE`; the executor never edits `codex/` on `feat/spec-compliance` in place, never touches the parent `.agents/plans/` tree during replay except the cycle's own validation artifacts, and never touches any explicit non-target.
- [ ] `TARGET_BRANCH` is created from `UPSTREAM_HEAD_SHA` (`1fda843fbc`) exactly; `git merge-base 1fda843fbc HEAD` returns `1fda843fbc`.
- [ ] Replayed commit inventory in `UPSTREAM_HEAD_SHA..HEAD` is exactly 14 commits in topological order matching the replay-set rows, identified by cherry-pick provenance trailers (or stable patch-id equivalence where conflict edits were needed). Source-SHA equality against a replayed commit is forbidden as evidence (§2.6).
- [ ] Replay-set artifact present, schema-valid (§2.4 field order), 14 rows; its canonical checksum (below) verified before replay and unchanged at seal.
- [ ] Baseline artifact created before any replay commit; all rows carry non-null `provenance_ref`; must-be-green rows green; `r02` existence probe = `missing-as-expected`.
- [ ] Exception ledger exists (empty or with approved rows); no unresolved exceptions at seal.
- [ ] Zero `manual_review` rows and zero other hard-blocking rows (classification: all 14 `required_runtime`).
- [ ] Post-replay canonical validation set green in the worktree/side repo; no net-new failures vs baseline; must-be-green rows green.
- [ ] Change-scope: worktree `git diff --name-status 1fda843fbc..HEAD` lists only paths in the union of replay-set `target_paths`; generated-artifact exclusion probe returns 0 (§2.7).
- [ ] Spec-immutability (§1.13 gate 12): `docs/agent-specs/forward-port-spec.md` exists and this cycle did not touch it — assert the §1.10-recorded `sha256sum` re-matches (the spec is an enumerated pre-existing dirty path, so the diff-empty form is replaced by the hash re-assertion form).
- [ ] E2E gate rows are bound (from `docs/codex-e2e-runbook.md`) and marked `not-run-in-pressure-test`; no PASS claimed, no bypass exception needed (live e2e is out of scope, §1.15 rehearsal scope).
- [ ] Reviewer+judge approvals recorded (Approval section; standing-order routine landing).
- [ ] §1.19 cleanup rows executed on the run's realized path (seal or STOP): scratch worktree, symlink shim, and any rehearsal scratch removed.

## Implementation Phases (§1.14 item 7)

Working-directory shorthands: `PARENT` = `/home/basil/projects/context-bonsai-agents`; `CODEX` = `/home/basil/projects/context-bonsai-agents/codex`; `WORKTREE` = `/home/basil/projects/context-bonsai-agents/codex/.agent_tmp/rebase-on-1fda843fbc`; `SIDE` = `/home/basil/projects/context-bonsai-agents/codex_context_bonsai`.

### Phase 0: Credentials preflight (§4.2 / §4.5 Credentials)

Live e2e is out of scope for this pressure test, so credentials are **not required to seal**. Bind the probe for completeness; a failure here is not a pressure-test blocker (record and continue):

- `codex login status` (exit status only; SOURCE-VERIFIED `codex-rs/cli/src/login.rs`). Provider named in records only — never keys or auth files.

### Phase 1: Preflight — toolchain, clean state, frozen identities, shallow-deepen (§1.10, §1.12, §2.1, §4.5)

1. Toolchain presence (§4.5): `cargo --version`; `rustup show` (fork + side crate both pin `channel = "1.93.0"` via their `rust-toolchain.toml`; cargo resolves per directory); `command -v jq`; `command -v sha256sum`. Bootstrap beyond toolchain: none — the first worktree build fetches crates itself.
2. Clean-state check (§1.10). From `PARENT`: `git status --short` must contain **only** these invoker-enumerated pre-existing dirty paths (untouchable — never stage/commit/revert):
   - ` M codex_context_bonsai`
   - ` M tweakcc_context_bonsai`
   - ` M docs/agent-specs/derivation-pipeline-spec.md`
   - ` M docs/agent-specs/forward-port-spec.md`
   - ` M scripts/detect-pending-target.mjs`
   - ` M scripts/detect-pending-target.test.mjs`
   - `?? docs/codex-e2e-runbook.md`
   Any non-enumerated dirty path fails preflight (`env-workspace-dirty`).
   (This plan's own new files — the plan and replay-set, the §1.15 rehearsal evidence logs `.agents/plans/validation/rehearsal-codex-5a680bcb3a.log` and `.agents/plans/validation/rehearsal-codex-sidecrate.log`, plus executor-created baseline/exceptions — are this cycle's artifacts, expected additions, not foreign dirt; the plan, replay-set, and rehearsal logs may already be committed by the time execution starts, in which case they no longer appear in status at all.)
3. Record spec-immutability hash for gate 12 (the spec is enumerated dirty, so the diff-empty assert cannot hold through no act of this cycle — §1.13 gate 12 exception). From `PARENT`: `sha256sum docs/agent-specs/forward-port-spec.md` — persist the value; re-assert identical at seal.
4. Shallow-deepen for merge-base (§4.5): from `CODEX`, `git rev-parse --is-shallow-repository`. If merge-base against `UPSTREAM_HEAD_SHA` fails "no common ancestor," deepen from `upstream` (`git fetch --deepen=<n>` or `git fetch --unshallow upstream`) until `git merge-base 1fda843fbc 5a680bcb3a` resolves — a merge-base failure on a shallow checkout is an `env-*` condition, not divergence.
5. Frozen-identity verification (§2.1 preflight). From `CODEX`:
   - `git rev-parse --verify refs/heads/feat/spec-compliance` → `5a680bcb3a5d63dffd7b77a4cdd414b2c951ab9c` (if it differs from the frozen `SOURCE_HEAD_SHA`, that is source drift → `input-source-drift`, generate a fresh plan per §1.9; do not reset the branch).
   - `git rev-parse --verify 1fda843fbcd825d92f41005060a181ebda615ff7` resolves (the frozen upstream base).
   - `BASE_SHA` recompute: `git merge-base 1fda843fbc 5a680bcb3a` must equal `1fda843fbcd825d92f41005060a181ebda615ff7` (a supplied `BASE_SHA` differing from this fails the cycle — `input-base-sha-mismatch`).
6. Replay-set checksum verify (§1.4). From `PARENT`:
   `test "$(jq -c < .agents/plans/validation/replay-set-5a680bcb3a5d63dffd7b77a4cdd414b2c951ab9c.json | sha256sum | cut -d' ' -f1)" = "f25faa7f0d9c2b91e4432fd001cc9994b01b224845521b0f2ba11ab3bc7440f1"`
7. Create the empty exception ledger if absent: `.agents/plans/validation/exceptions-5a680bcb3a5d63dffd7b77a4cdd414b2c951ab9c.json` = `[]`.

### Phase 2: Isolated worktree creation + path-dependency symlink shim (§2.5, §4.5 Naming)

8. From `CODEX`: `git worktree add -B replay/context-bonsai-on-codex-1fda843fbc /home/basil/projects/context-bonsai-agents/codex/.agent_tmp/rebase-on-1fda843fbc 1fda843fbcd825d92f41005060a181ebda615ff7`
9. In `WORKTREE`: `test "$(git rev-parse HEAD)" = "1fda843fbcd825d92f41005060a181ebda615ff7"`.
10. **Symlink shim** (before any build): `ln -s ../../codex_context_bonsai /home/basil/projects/context-bonsai-agents/codex/.agent_tmp/codex_context_bonsai`; verify `readlink -f` resolves to `SIDE`. This satisfies the `codex-rs/core/Cargo.toml` relative path dependency from the worktree location.

### Phase 3: Baseline capture (clean upstream base; no replay yet) (§1.6, §2.8, §4.5)

11. Create `.agents/plans/validation/baseline-5a680bcb3a5d63dffd7b77a4cdd414b2c951ab9c.json`. Row schema (field order §1.6): `row_id`, `command`, `frozen_upstream_head_sha`, `frozen_source_head_sha`, `exit_code`, `result`, `artifact_path`, `provenance_ref`. A missing required field hard-fails the phase; `BLOCKED` only for a genuinely-unavailable external dependency; no `n/a` placeholders. Rows (§4.5 baseline set):
    - `r01` (**must-be-green**), from `WORKTREE/codex-rs`: `cargo build --bin codex` (memory-constrained rigs: `CARGO_INCREMENTAL=0 CARGO_PROFILE_DEV_DEBUG=0 cargo build --bin codex`). Must be green on the clean upstream base — a failure is a pre-existing upstream regression → STOP + escalate (`env-upstream-regression`).
    - `r02`, from `WORKTREE` root: `test ! -f codex-rs/core/src/context_bonsai.rs && echo missing-as-expected || (echo unexpected-presence && false)`. The chain introduces this module; upstream base must not have it. `result` must be `missing-as-expected` (`unexpected-presence` hard-fails). Do not run a test against a path known absent. `provenance_ref`: `git ls-files codex-rs/core/src/context_bonsai.rs` (empty on the base).
    - `r03` (**must-be-green**), from `SIDE` (the real side repo — **not** part of the worktree): `cargo test`.
    - `r04`, from `SIDE`: `cargo fmt --check`.
    - `r05` (**must-be-green**), from `SIDE`: `cargo clippy --all-targets --all-features`.
12. Baseline hard-fail rule: only a missing/null required field fails Phase 3. Non-zero `exit_code` is data — except `r01`/`r03`/`r05` (must-be-green) and `r02` (must be `missing-as-expected`).

### Phase 4: Replay — cherry-pick in topological order (§2.5)

Conflict scope: each row's conflict-resolution edits must stay inside that row's `target_paths` (as bounded by the allowlists). An edit outside that scope is an out-of-scope fixup → STOP, add an exceptions-ledger row citing the authorizing replay-set row + reviewer/judge approval refs, then continue. No conflicts are anticipated (base == port base).

13. Cherry-pick each of the 14 rows **in the replay-set / topological order**, with provenance:
    `git cherry-pick -x <source_sha>` for, in order: `5cad90f9`, `26e6abb7`, `d94efcf1`, `96bcf61e`, `a17bc74e`, `c9e69c0f`, `fd56b70e`, `6b2f5a43`, `1beb7b2d`, `689c4e1f`, `1863ca28`, `8e62af86`, `78af9df2`, `5a680bcb`.
14. Provenance-trailer count (anchored grep; §2.6; run identically here and at seal), from `WORKTREE`:
    `test "$(git log --format='%b' 1fda843fbcd825d92f41005060a181ebda615ff7..HEAD | grep -cE '^\(cherry picked from commit [0-9a-f]{40}\)$')" = "14"`

### Phase 5: Post-replay canonical validation set (§4.5)

15. Run the canonical validation set:
    - From `WORKTREE/codex-rs`: `cargo build --bin codex`
    - From `WORKTREE/codex-rs`: `cargo test -p codex-core context_bonsai` (replayed module, tool-handler, reconstruction tests)
    - From `SIDE`: `cargo test` (r03), `cargo fmt --check` (r04), `cargo clippy --all-targets --all-features` (r05)
    - Offline registration probe, from `WORKTREE`: `strings codex-rs/target/debug/codex | grep -E 'context-bonsai-(prune|retrieve)'` (build-level only; runtime registration is an e2e concern — not run here).
16. Baseline-delta gate: post-replay `r01`/`r03`/`r05` must be no worse than baseline; net-new failures are hard-fail regressions.
17. Change-scope + generated-artifact checks, from `WORKTREE`:
    - `git diff --name-status 1fda843fbcd825d92f41005060a181ebda615ff7..HEAD` — every path must be in the union of replay-set `target_paths`. An out-of-scope path landed without a prior exception → STOP + revert (`executor-scope-violation`).
    - `git diff --name-status 1fda843fbc..HEAD | grep -E '(\.d\.ts$|/generated/|/__generated__/|openapi|\.gen\.|/dist/)' | wc -l` must be `0` (§2.7). Any match → STOP + remove from replay commits.

### Phase 6: E2E gate binding — bound, not run in pressure test (§1.15 rehearsal scope, §4.5, `docs/codex-e2e-runbook.md`)

Live e2e is **out of scope** for this pressure test. Commands below are **copied from `docs/codex-e2e-runbook.md` only** (no invention) and every row is marked `not-run-in-pressure-test`. No PASS is claimed and no bypass exception is required (a bypass exception + reviewer/judge approval would be needed only if a PASS were being *waived*; here the whole gate is out of scope by the cycle's pressure-test definition).

Required scenario set for a future live release-gate PASS (§4.5, immutable per cycle) — bound here, `not-run-in-pressure-test`:
- **E2E-01** contiguous prune — runbook Part 1 "E2E-01/E2E-02"; first-turn drive `target/debug/codex exec --json "<prompt>" > /tmp/codex-bonsai-e2e/prune-invented.jsonl 2>…`, follow-up `codex exec --json resume <session-id>`; archive inspector `grep -l '"bonsai_archive"' ~/.codex/sessions/*/*/*/rollout-*.jsonl` then `jq`.
- **E2E-02** ambiguity rejection — runbook Part 1 same section (deterministic rejection, no anchor id, no new archive entry).
- **E2E-03** retrieve by anchor — runbook Part 1 "E2E-03"; resume turn passing the exact E2E-01 anchor id.
- **E2E-07 / Protocol A** secret-prune oracle — runbook Part 1 "E2E-07"; seed a code word, prune the seeding span, ask for it back without quoting it; PASS = non-empty non-leaking recall + archive entry + secret-free tool args.
- **E2E-04 (gauge)** and **E2E-06 (persistence across resume)** — runbook Part 1 "Open live scenarios"; **the first routine cycle reaching the live gate must run both; no full parity claim exists until they pass**. Bound, `not-run-in-pressure-test`.
- Build/registration probes — runbook "Build and offline registration probe" (`cargo build --bin codex`; `strings … | grep`) and "Live registration check" (`timeout 240 target/debug/codex exec --json "List your available tool names…"`); the offline probe is covered in Phase 5; the live registration check is `not-run-in-pressure-test`.

Evidence directory `mkdir -p /tmp/codex-bonsai-e2e` and its removal (`rm -rf /tmp/codex-bonsai-e2e`) are runbook-bound but not exercised here (nothing captured). E2E-05 stays non-live per its §4.5 justification (fail-closed path covered by the side-crate `cargo test`).

### Phase 7: Seal gates (pressure-test mode) — working-tree verification, no push (§1.13 as bound by §1.11)

Assert each; hard-fail on any miss. All verification is in `WORKTREE`/`SIDE`/`PARENT`; **no `git push`, no tag creation, no pin advance** (§2.9 out of scope).

18. Gate 1 — every inventory item has exactly one replay-set row: 14 inventory items, 14 rows (zero unmapped).
19. Gate 2 — zero unresolved `manual_review` / hard-blocking rows (all 14 `required_runtime`).
20. Gate 3 — zero unapproved late fixes; no drift (Phase 1 step 5 confirmed).
21. Gate 4 — replay-set present, schema-valid, checksum re-verified unchanged: re-run the Phase-1 step-6 command; must still equal `f25faa7f0d9c2b91e4432fd001cc9994b01b224845521b0f2ba11ab3bc7440f1`.
22. Gate 5 — no separate approval artifacts required (no hard-blocking rows; no manual-review-approvals file this cycle).
23. Gate 6 — baseline artifact complete: schema-valid rows, no forbidden placeholders, `provenance_ref` present on every row.
24. Gate 7 — replay verification by provenance: re-run the Phase-4 step-14 trailer-count assert (== 14). Final-state working-tree checks (§2.6): `git merge-base 1fda843fbc HEAD` == `1fda843fbc`; `git rev-list --count 1fda843fbc..HEAD` == `14`; `git log --format='%s' 1fda843fbc..HEAD` subjects match the replay set.
25. Gate 8 — post-replay validation no worse than baseline; must-be-green rows (`r01`, `r03`, `r05`) green.
26. Gate 9 — change-scope: the Phase-5 `git diff --name-status` union check passes; any out-of-scope landing without a prior exception → STOP + revert.
27. Gate 10 — no unresolved exception records (ledger empty or all rows resolved).
28. Gate 11 — E2E release gate: **not applicable in pressure-test scope** — the gate is bound `not-run-in-pressure-test` (Phase 6); no PASS is asserted and no bypass exception is recorded (per the cycle's §1.11/§1.15 pressure-test definition, live e2e is out of scope, not waived).
29. Gate 12 — spec immutability: `test -f docs/agent-specs/forward-port-spec.md` AND re-assert the Phase-1 step-3 `sha256sum` value is unchanged (the diff-empty form is replaced by the hash form because the spec is an enumerated pre-existing dirty path).
30. Gate 13 — reviewer + judge approvals recorded (see Approval).
31. Gate 14 — §2.9 release-gate ladder: **not run** (pressure-test; no push/tag/pin/fast-forward). Push-readiness dry-runs (§2.6) are also out of scope since nothing is pushed.

### Phase 8: Cleanup — §1.19 scratch-state lifecycle (on BOTH seal and STOP paths)

The removal set is exactly the paths this run creates outside the repositories. Runs are serial and `/tmp`/scratch is shared — never remove another run's state; a foreign path in the way is an invoker disposition, not a cleanup target.

- **On seal** (after all gates pass; nothing durable to record since no branch/tag is pushed in pressure test): remove
  - the scratch worktree: `git -C /home/basil/projects/context-bonsai-agents/codex worktree remove --force /home/basil/projects/context-bonsai-agents/codex/.agent_tmp/rebase-on-1fda843fbc`
  - the symlink shim `codex/.agent_tmp/codex_context_bonsai` (removed with the worktree dir; if it survives, `rm /home/basil/projects/context-bonsai-agents/codex/.agent_tmp/codex_context_bonsai`)
  - the scratch branch `replay/context-bonsai-on-codex-1fda843fbc` (`git -C codex branch -D …`) — it is scratch, never pushed
  - any §1.15 rehearsal scratch worktree (created under a name distinct from `TARGET_WORKTREE`) **if still present** — the generation-time loop-closer removes it when the §1.15 loop closes, so this row is normally a no-op; `git -C codex worktree list` decides, and an already-absent path is not a STOP condition
  - `rm -rf /tmp/codex-bonsai-e2e` only if Phase 6 had created it (it does not in pressure test)
- **On STOP**: the run's scratch is run evidence — **preserve** the worktree, symlink, branch, and any rehearsal scratch until the STOP is dispositioned, then the disposition act removes them. Record the STOP reason code (§1.17) in the final and §1.16 maintenance reports.
- The parent-repo validation artifacts (plan, replay-set, baseline, exceptions) are durable cycle records, **not** cleanup targets.

## Validation Commands, grouped by working directory (§1.14 item 8)

### From `PARENT`
- `git status --short` (only the enumerated dirty paths + this cycle's own new artifacts)
- `sha256sum docs/agent-specs/forward-port-spec.md` (record at preflight; re-assert at seal — gate 12)
- `jq -c < .agents/plans/validation/replay-set-5a680bcb3a5d63dffd7b77a4cdd414b2c951ab9c.json | sha256sum | cut -d' ' -f1` → `f25faa7f0d9c2b91e4432fd001cc9994b01b224845521b0f2ba11ab3bc7440f1`
- `test -f docs/agent-specs/forward-port-spec.md`

### From `CODEX` (read-only inventory + frozen identities)
- `git rev-parse --is-shallow-repository`
- `git rev-parse --verify refs/heads/feat/spec-compliance` → `5a680bcb3a5d63dffd7b77a4cdd414b2c951ab9c`
- `git rev-parse --verify 1fda843fbcd825d92f41005060a181ebda615ff7`
- `git merge-base 1fda843fbc 5a680bcb3a` → `1fda843fbcd825d92f41005060a181ebda615ff7`
- `git log --topo-order --reverse --format='%H|%P|%s' 1fda843fbc..5a680bcb3a` (14 rows)
- `git log --name-status --find-renames --format='%H|%s' 1fda843fbc..5a680bcb3a`
- `git cherry -v 1fda843fbc 5a680bcb3a` (14 `+`)
- `git log --merges 1fda843fbc..5a680bcb3a` (empty)

### From `WORKTREE` root / `WORKTREE/codex-rs`
- `git rev-parse HEAD` → `1fda843fbc` (before replay)
- `cargo build --bin codex` (from `codex-rs`; r01 / post-replay)
- `cargo test -p codex-core context_bonsai` (from `codex-rs`; post-replay)
- `strings codex-rs/target/debug/codex | grep -E 'context-bonsai-(prune|retrieve)'`
- `test "$(git log --format='%b' 1fda843fbc..HEAD | grep -cE '^\(cherry picked from commit [0-9a-f]{40}\)$')" = "14"`
- `git merge-base 1fda843fbc HEAD` → `1fda843fbc`
- `git rev-list --count 1fda843fbc..HEAD` → `14`
- `git log --format='%s' 1fda843fbc..HEAD` (subjects match replay set)
- `git diff --name-status 1fda843fbc..HEAD` (⊆ replay-set `target_paths` union)
- `git diff --name-status 1fda843fbc..HEAD | grep -E '(\.d\.ts$|/generated/|/__generated__/|openapi|\.gen\.|/dist/)' | wc -l` → `0`

### From `SIDE` (`codex_context_bonsai/`)
- `cargo test` (r03, must-be-green)
- `cargo fmt --check` (r04)
- `cargo clippy --all-targets --all-features` (r05)

## E2E Gate section (§1.14 item 9)

Procedure: `codex_context_bonsai/docs/e2e-testing.md` (combined installation+runtime). Command binding: `docs/codex-e2e-runbook.md` — cycle plans copy commands from it; if the runbook lacks a needed command, generation flags the gap rather than inventing (§4.5 rule; no gap encountered). Required minimum scenario set for a live release-gate PASS (§2.9, immutable per cycle): E2E-01, E2E-02, E2E-03, E2E-07/Protocol A (all EXECUTED 2026-05-20 at derivation) **plus the two open live scenarios E2E-04 (gauge) and E2E-06 (persistence across resume)** — bound but never executed live; the first routine cycle reaching the live gate must run them and no full parity claim exists until they pass. **In this pressure-test cycle the entire gate is `not-run-in-pressure-test`** (Phase 6): live e2e is out of scope, so the gate is bound (commands copied, scenario set enumerated) but not exercised, and gate 11 is satisfied by the out-of-scope binding, not by a PASS or a bypass exception. Evidence-recording target for a future live run: `codex_context_bonsai/docs/e2e-results-<DATE>-live.md` (2026-05-20 precedent); rollout files under `~/.codex/sessions/**` are read for evidence only, never committed.

## Worktree Artifact Check (§1.14 item 10)

- Checked at generation: 2026-07-05 (owner-tier generator).
- **Parent-repo generator writes**: `.agents/plans/story-rebase-cycle-5a680bcb3a5d63dffd7b77a4cdd414b2c951ab9c.md` (this file); `.agents/plans/validation/replay-set-5a680bcb3a5d63dffd7b77a4cdd414b2c951ab9c.json`. Overlap check: no pre-existing artifact at either path keyed to this `SOURCE_HEAD_SHA` (`tracked-dirty`: none; `existing-untracked`: none) — no §1.14 same-cycle collision. Other `.agents/plans/**` artifacts belong to other cycles' SHAs and do not collide.
- **Parent-repo executor writes**: `.agents/plans/validation/baseline-…json`, `.agents/plans/validation/exceptions-…json` — do not yet exist; the executor re-runs this check immediately before its first write.
- **Worktree/side-repo targets** (replay targets, not direct edits): the replay-set `target_paths` union inside `codex-rs/**`; created fresh in the new worktree. `TARGET_WORKTREE` and `TARGET_BRANCH` do not currently exist. The symlink shim path `codex/.agent_tmp/codex_context_bonsai` already exists (created by the completed §1.15 rehearsal, content-identical to what Phase 2 would create — verify with `readlink` and reuse; do not treat as a collision).
- Prior cycles' worktrees (if any under `codex/.agent_tmp/`) are untouchable; none overlap `rebase-on-1fda843fbc`.
- Escalation status: none. Decision citation: owner-tier generation under the Stage 6 acceptance gate (`derivation-pipeline-spec.md` §9).

## Plan Approval and Commit Status (§1.14 item 11)

- **Approval Status**: approved (routine local landing). This is a generation-only pressure-test plan; no outward/destructive action is taken.
- **Approval Citation**: standing owner order 2026-07-05 — local landings are routine and proceed autonomously (owner directive: in-scope design/spec decisions proceed without parking on Basil; only outward/destructive actions wait). Reviewer+judge gates are satisfied by the owner-tier generation + §1.15 validation loop; their citations are recorded in Validation Loop Results when the loop runs.
- **Plan Commit Hash**: the commit introducing this file and the replay-set (resolve with `git log --follow --oneline -1 -- .agents/plans/story-rebase-cycle-5a680bcb3a5d63dffd7b77a4cdd414b2c951ab9c.md`).
- **Ready-for-Orchestration**: YES — the §1.15 loop closed at iteration 1 with all blocking findings fixed in this plan (rehearsal GREEN; missing-details and ambiguity passes recorded in Validation Loop Results).

## Validation Loop Results (§1.15)

**Iteration 1 — target-resolution rehearsal (2026-07-06, owner tier): GREEN.** Disposable scratch worktree `codex/.agent_tmp/rehearsal-1fda843fbc` at `UPSTREAM_HEAD_SHA` (distinct from `TARGET_WORKTREE`), symlink shim `codex/.agent_tmp/codex_context_bonsai` → `../../codex_context_bonsai` created first and verified (`readlink -f` from `codex-rs/core/../../../` resolves to the real side crate). Full replay sequence applied: `git cherry-pick 1fda843fbc..feat/spec-compliance` — all 14 rows clean, zero conflicts. Post-replay validation rows: `cargo build --bin codex` green (2m23s, plain command, no rig accommodation needed); `strings target/debug/codex | grep -cE 'context-bonsai-(prune|retrieve)'` = 10 matches; `cargo test -p codex-core context_bonsai` = 38 passed, 0 failed (lib target). Side-crate rows from `codex_context_bonsai/`: `cargo test` green (3+7 passed across targets); `cargo clippy --all-targets --all-features` green; `cargo fmt --check` fails at committed HEAD `5235cd3` (233-line pre-existing formatting drift under the pinned 1.93.0 toolchain — recorded as the §4.5 r04 informational baseline, not a rehearsal failure; no new drift is introduced by this cycle). Evidence logs: `.agents/plans/validation/rehearsal-codex-5a680bcb3a.log`, `.agents/plans/validation/rehearsal-codex-sidecrate.log`. Scratch worktree removal per §1.19 recorded below when the loop closes.

**Iteration 1 — missing-details pass** (independent, repository-inspecting): 2 blocking + 1 minor, all fixed in this plan. (a) Phase-1 clean-state enumeration omitted the two rehearsal evidence logs → added to the expected-additions parenthetical. (b) `r05` (side-crate clippy) is must-be-green per §4.5 but the plan's Phase-3 hard-fail rule and Gate 8 listed only `r01`/`r03` → `r05` added everywhere. (c) The §1.14 overlap check claimed the symlink shim "does not currently exist" while the completed rehearsal had created it → corrected to reuse-not-collision. Verified clean by the reviewer: replay-set checksum, all 14 SHAs, frozen identities, runbook command citations, working directories, §1.19 cleanup-row coverage.

**Iteration 1 — ambiguity pass** (independent, repository-inspecting): 1 blocking + 2 minor, all fixed. (a) Same rehearsal-log enumeration gap (STOP-vs-proceed divergence) → same fix. (b) Dual ownership of rehearsal-worktree removal (generation-time loop-closer vs Phase 8) → bound: the loop-closer removes it at loop close; Phase 8's row is an idempotent if-still-present sweep. (c) Stale "loop not yet run" in Ready-for-Orchestration → updated. Verified clean: no source drift (`feat/spec-compliance` still at `5a680bcb3a`), checksum match.

**Loop closed at iteration 1** (zero blocking findings remaining after fixes; rehearsal GREEN on the first pass). Loop-close cleanup per §1.19: rehearsal worktree `codex/.agent_tmp/rehearsal-1fda843fbc` removed (`git worktree remove --force` + `git worktree prune`); the symlink shim is retained deliberately for the execution phase (see §1.14 item 10 — content-identical to what Phase 2 creates).

## Completion Checklist (mirrors the seal gates)

- [ ] Every inventory item mapped to exactly one replay-set row (14/14)
- [ ] Zero unresolved `manual_review` / hard-blocking rows
- [ ] Zero unapproved late fixes; no source drift
- [ ] Replay-set present, schema-valid, checksum verified pre-replay and unchanged at seal
- [ ] Baseline artifact complete; no forbidden placeholders; `provenance_ref` on every row; must-be-green rows green; `r02` = missing-as-expected
- [ ] Replay verified by provenance trailers (14) + working-tree final-state checks; no source-SHA equality used
- [ ] Post-replay validation no worse than baseline; must-be-green rows green
- [ ] Change-scope union check passes; generated-artifact probe = 0
- [ ] No unresolved exception records
- [ ] E2E gate bound and marked not-run-in-pressure-test (no PASS, no bypass exception)
- [ ] Spec immutability: file exists + preflight `sha256sum` re-asserted unchanged (this cycle did not touch the spec)
- [ ] Reviewer + judge approvals recorded
- [ ] §2.9 ladder confirmed out of scope (nothing pushed/tagged/pinned)
- [ ] §1.19 cleanup rows executed on the realized path (seal or STOP)

## §1.16 Maintenance duties (run after cycle end — sealed or STOP)

The executor performs routine maintenance and records the outcome (a STOP does not waive it):
- Absorb slot-level facts that changed into §4.5 with cited evidence; escalate any core/shape (Parts 1–3) gap to the owner tier. The routine executor edits Part 4 only; the edit is left uncommitted and its path named in the final report (§1.16 disposition rule) — until owner review lands, the next cycle receives the edited file as an enumerated dirty path.
- Give every stumble a `SPEC-GAP` vs `EXECUTOR-FAIL` verdict.
- **Candidate SPEC-GAPs surfaced at generation (record verdicts / consider Part 4 fixes)**:
  1. **`codex-rs/Cargo.lock` outside the §4.5 runtime allowlist** — two port commits touch the fork workspace lockfile; the allowlist enumerates only the six fork crate dirs + the side-crate `Cargo.lock`. Candidate Part 4 fix: add `codex-rs/Cargo.lock` (and possibly `codex-rs/Cargo.toml`) to the runtime allowlist. `SPEC-GAP` (harness-local slot fix).
  2. **§1.11 `uncommitted-pressure-test` semantics vs the git-fork shape** — the mode's literal "HEAD may equal upstream / working-tree commands" language is written for the in-place closed-artifact shape; git-fork replay creates scratch-worktree commits. This plan binds it as "unpushed, no durable ref landed, worktree removed at seal." Core/shape ambiguity → escalate to owner tier (`SPEC-GAP`, Part 1/§1.11).
  3. **Raw frozen commit standing in for §2.1's release tag** — §2.1 mandates a release tag (or explicitly-targeted branch ref); a bare commit SHA is neither. Task-authorized for this pressure test; if pressure tests against a between-releases base become routine, §2.1/§1.11 should bind the raw-commit case explicitly. Core/shape → escalate (`SPEC-GAP`, §2.1).
- If no slot-level fact changed, record that explicitly.
